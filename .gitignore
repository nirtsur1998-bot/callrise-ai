node_modules
dist
out
.DS_Store
.eslintcache
*.log*
*.tsbuildinfo

# Native addon build output (run `npm run native:build:mac` to rebuild) - source (binding.gyp, src/*.mm) is tracked.
native/*/build

# Secrets — never commit your API keys
.env
.env.local
.env.*.local
