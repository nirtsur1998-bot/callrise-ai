# CallRise AI — Project Guide

> ## ▶ NEXT SESSION STARTS HERE — 2026-08-18
>
> **One task is queued; everything else is done, committed and pushed:
> wire `recorder.ts` for Tier 1, verify four properties, then ship 1.3.0.**
>
> - **Read first:** [`docs/M27-tier1-recorder-handoff.md`](docs/M27-tier1-recorder-handoff.md)
>   — settled interfaces, the four properties with what "not vacuous" means for
>   each, and the ship sequence.
> - **Commands to paste:** [`docs/M27-RUNBOOK.md`](docs/M27-RUNBOOK.md) — checkpoint,
>   merge, verify, publish. **Elevated steps** (clean-slate install/driver
>   verification) are separate, ready-to-paste blocks in
>   [`docs/M27-ELEVATED-STEPS.md`](docs/M27-ELEVATED-STEPS.md) — run those in
>   your own Administrator shell, never asked of the assistant.
> - **Branch:** `claude/m27-field-hardening` (pushed). Working tree clean.
> - **Edit exactly one file's wiring:**
>   `src/renderer/src/features/live/audio/recorder.ts`. Everything it needs
>   already exists and is tested (`tier1-source.ts`, `denoised-source.js`,
>   `window.api.tier1`).
>
> **The property that matters most: _raw mic never disconnected._** It is the
> only failure in this release that costs a **recorded call** rather than
> degraded audio — and it is exactly the shape that gets asserted vacuously.
> Assert on `micTrack.readyState`, `stream.active`, and a spy proving `stop()`
> was never called. Then break the code on purpose and watch **that specific
> assertion** go red before trusting it.
>
> **State of the feature right now:** the engine is verified correct and the
> preload API exists — but **nothing calls it**. Until `recorder.ts` is wired,
> Tier 1 does nothing for a user. That is the whole remaining gap.

## What we're building

CallRise AI is a desktop application that acts as an AI assistant for sales calls. Today it is a thin, static UI shell. Over time it will grow into a tool that listens to live calls, transcribes them in real time, and coaches the rep with in-the-moment suggestions — alongside a CRM, tasks, calendar, analytics, coaching, and a knowledge base. The long-form product vision lives in [`docs/VISION.md`](docs/VISION.md).

## Progress

Milestones completed:

- **M1 — App shell.** ✅ Electron + React + Tailwind desktop shell (dark Linear/Raycast layout: sidebar nav, main view, copilot panel).
- **M2 — Live transcription.** ✅ Real-time microphone transcription via Deepgram Nova-3, with speaker labels.
- **M3 — Saved calls.** ✅ Calls saved to disk with speaker diarization; Past Calls list + detail view.
- **M4 — AI summaries.** ✅ Claude summaries of saved calls and attached files (PDF/txt/md/docx), via the main-process `.env` relay.
- **M5 — AI tasks.** ✅ Generate action items from a saved call (review/edit/accept) + a real Tasks screen (list, filters, complete/edit/delete/add).
- **M6 — User accounts + email auth.** ✅ Sign-up / log-in gate via **Supabase Auth**, with email confirmation by a 6–10-digit code (no redirects/deep links). Auth runs in the main process (keys in the gitignored `.env`); the signed-in user shows in the sidebar with log-out.
- **M7 — In-app calendar (foundation).** ✅ Custom month + week grid (date-fns; no calendar library), local event storage (one JSON file per event, mirroring tasks/calls), create/edit/delete events, and a read-only overlay of tasks (on due dates) + past calls (on the day/time they happened). Events store `source: 'local'` + empty `provider`/`externalId` hooks and absolute ISO times so the future Google/Outlook sync needs no migration. **No external sync yet** (deliberately deferred).
- **M8 — AI post-call coaching (scorecard).** ✅ "Coach this call" on a saved call → a Claude-scored, evidence-grounded scorecard: 6 rubric dimensions (1–5), a computed 0–100 Call score, deterministic metrics (talk ratio, longest monologue, question count, pace, turns), a lead strength, top-2 prioritized improvements (mechanical + strategic), and one next-call action (savable as a task). Every cited quote is **verified against the transcript** and ungrounded advice is dropped. Persisted on the call (`coaching` field); the **Coaching** screen lists coached calls. **No live/in-call coaching yet** (that's M9).
- **M9 — Live in-call coaching (foundation).** ✅ Real-time cues on the **Live Calls** screen. On each completed client turn (single-flight, ~400ms-debounced, fail-fast on 429/529 rate limits), a fixed speaker-labeled transcript window goes to **Claude Haiku**, which identifies the rep and returns ONE glanceable, grounded cue — **objection / discovery / next-question / buying-signal** — about what the client said; plus a **rep-only** deterministic "slow down". Rep is auto-identified from their self-intro and locked for the call; **mute** + Low/Med/High sensitivity; one cue at a time, dismissible. Also a manual **"Ask the coach"** box that sends the running transcript for in-context help. Reuses M8's coaching standards; typical cue latency ~0.5s.
- **M10 — Analytics dashboard.** ✅ A read-only **Analytics** screen that turns data already on disk into meaning — **no new data, no AI, no new storage**; all aggregation is deterministic (counting/averaging), so it works offline and instantly. Reuses the existing IPC (`calls.list`/`calls.get`/`tasks.list`). Five cards: activity over time (calls per week/month), talk-to-listen ratio, per-skill coaching scores, "where to focus" (lowest non-green skills + a concrete action), and task follow-through. Every card answers "so what?" — a green/amber/red **health tone** (reusing the coaching `Tone` tokens), a one-line plain-English **verdict**, and a hand-rolled SVG/CSS bar (no chart dependency). A single deterministic **headline** picks the biggest takeaway. Metrics from fewer than 3 coached calls are caveated ("early days — based on N calls"). Logic lives in `features/analytics/` (`aggregate.ts` = data, `verdicts.ts` = meaning, `charts.tsx` = bars).
- **M11 — Recording-consent foundation.** ✅ The required safety layer **before** any buyer-side capture (M12). Each call carries a `ConsentRecord` (status `not-asked`/`disclosed`/`consented`/`declined`, jurisdiction one/two-party, how consent was obtained, timestamps, and a `recordOtherParty` flag), stored on disk with the call — **no new storage**. **Hard invariant:** `recordOtherParty` is only ever true when `status === 'consented'` — recomputed in the main process `sanitizeConsent` on **every save AND every read**, so a hand-edited/malformed file can't grant capture. This is the flag M12 capture must gate on ("no consent = no capture"). UI (`features/consent/`): an **"Other party" control** in the live bar that can't flip on directly — only the **consent modal** (gate + editable disclosure script + how-consent-was-obtained + jurisdiction; default **two-party**) can enable it via an explicit "they said yes". Consent **resets to off after every call**. A persistent, **honest recording indicator** says "Recording — your mic" while mic-only (with a "buyer capture arrives in M12" note when consent is on); the "you + the other party" variant is built but stays OFF until M12. One-line guardrail notes that consent laws vary by location. **No audio capture of the other party in this milestone** — mic capture is unchanged.
- **M12 — Buyer-side audio capture + dual-side transcription.** ✅ Captures the other party via **system-audio loopback** (`getDisplayMedia` + Electron's `audio: 'loopback'` response — CoreAudio-backed on macOS, WASAPI-backed on Windows; no external driver, no native code needed on either platform), merged with the mic through one `AudioContext`/`ChannelMergerNode` into interleaved stereo, streamed over **one Deepgram multichannel socket** → deterministic **You (ch 0) / Buyer (ch 1)** labels (no diarization guessing). **Consent-gated & double-gated:** buyer capture opens only from the consent modal's "they said yes" **click**, and the main-process display-media handler **denies** any request that wasn't armed after consent — capture is gated in main as well as the renderer. **Retention backstop:** the main process **strips buyer turns on save, read, and list** unless consent still permits it (so revoking mid-call, or a hand-edited file, never surfaces the other party). Honest recording indicator (**"you + the other party"** only when buyer audio is truly streaming) + failure recovery (Screen-Recording settings deep-link on macOS; no equivalent needed on Windows). New Home **"Audio sources"** section picks the recording mic and shows the current output with a headphones reminder. Live cues get the known rep channel. Built in de-risked steps (packaged-build permission check → Deepgram-multichannel probe → stereo worklet → socket plumbing → consent wiring → retention → recovery UI → cue labels), verified with adversarial bug-hunt + runtime proofs. **Originally macOS-only; extended to Windows on 2026-07-27 by loosening the platform gates in `main/loopback.ts` / `useTranscription.ts` / `OtherPartyControl.tsx` to also allow `win32`, since the underlying Electron capture API was never actually macOS-specific.** **Windows verification, 2026-07-28:** tested on real Windows hardware. Generic system audio (e.g. a played video) is captured correctly and split into the right You/Buyer channels — the cross-platform loopback mechanism itself works. A real VoIP call (tested: WhatsApp) was **not** reliably captured on the Buyer channel, though — the buyer's voice only came through via physical mic bleed (speaker output picked up by the mic, mislabeled as "You"), not genuine loopback capture. Best current explanation: Windows routes VoIP/communications-app audio through a separate "Communications" audio role that whole-system WASAPI loopback (what `audio: 'loopback'` taps here) doesn't reliably include the same way it includes ordinary media playback — this looks like an OS/Chromium-level limitation, not a bug in this code, and isn't fixable without a new native addon implementing Windows' newer per-process "Application Loopback Capture" API (see the "Not yet done" list — out of scope for now). **Bottom line: mic-only recording and the Windows launch are solid; buyer-side capture on real VoIP calls on Windows is not yet reliable.**
- **M13 — Google Calendar sync, READ-ONLY (step 1 of the calendar-sync arc).** ✅ Connect Google Calendar and pull the user's meetings INTO the app's calendar, read-only — no writing to Google, no Outlook, no conflict logic (those are M14/M15). The user authorizes in their **own system browser** (loopback + PKCE; the app never sees the password); the refresh token is stored **encrypted via `safeStorage`** (macOS Keychain) in the main process, never exposed to the renderer. Requested scope is `calendar.readonly` (read-only). Events are pulled (30 days back → 90 forward, all calendars, recurring expanded, cancelled dropped) into a **separate read-only cache** (`google-cache/`), mapped to the existing `CalendarEvent` shape with `source:'google'` + `provider:'google:<calId>'` + `externalId` (the match key M14 will use), and shown on the calendar grid as **green, read-only chips**. Refresh on calendar-open + a manual Refresh (no background polling); clean disconnect/reconnect. Lib: `google-auth-library` only. Security-reviewed (read-only, encrypted tokens, safe OAuth all verified). Setup (Google Cloud project, consent screen, Desktop OAuth client, credentials in `.env`) is done by the user in Google's own screens.
- **M14 — Two-way Google Calendar sync (push out + editable Google events + all-day).** ✅ Built on M13's read-only pull. A **separate "Enable two-way sync"** button runs its own consent flow for the `calendar.events` write scope (the read-only path is never silently widened); a persisted `sync-mode.json` gates every write, so **no write is possible in read-only mode**. App events now push **out** to Google (create/edit/delete), targeting each event's **own calendar** (owner/writer only — the pull records `accessRole`, so read-only calendars like holidays stay read-only). Local store stays source of truth; pushes are **best-effort** (offline/errors never block the local write) with a `sync` lifecycle (`local-only`/`synced`/`dirty`/`deleted`/`error`), **idempotent create** (deterministic Google id → crash-retry can't duplicate), tombstone deletes, and a **reconcile()** retry pass. **No duplicates:** a pushed event is stamped with the concrete `(provider, externalId)` and the pulled copy is deduped away (shows once as the editable local event). **Editable Google events:** green chips on writable calendars can be edited/deleted — editing "adopts" the event (a linked local copy that PATCHes the same Google event). **All-day + multi-day:** the New/Edit dialog gained an "All day" toggle + start/end dates, so those events round-trip correctly (verified across non-UTC timezones). Per-event serialization (`pushChains`/`stateLocks`) makes concurrent edits/deletes + reconcile races safe. Verified with adversarial bug-hunts + timezone runtime proofs. **Deferred:** Google→app change-flow-back + conflict _resolution_ (only the safe parts are done — the app never deletes local events from a pull); recurring-event editing; a multi-day _timed_ event shows its start time on each spanned day in month view (cosmetic).
- **M15 — Outlook Calendar sync.** ✅ Adds Outlook/Microsoft 365 Calendar on the **same read-only-pull + two-way-push engine** M13/M14 built for Google: OAuth via MSAL (loopback + PKCE, no client secret), Microsoft Graph calls for list/pull/push/delete, and a small provider dispatcher (`calendar-sync.ts`) so `events.ts` stays agnostic to which provider an event is linked to. Only one of Google/Outlook is expected to be in two-way sync at a time. Same deferred item as M14: Google/Outlook→app conflict _resolution_ for simultaneous two-sided edits is still open.
- **M16 — Cloud backup (per-user Supabase mirror).** ✅ Opt-in, per-user backup of local data to Supabase so it can be restored on a new machine — **local files stay the source of truth**; **raw transcripts never leave the device** (calls sync as metadata/summary/quote-free coaching only). Built in 7 steps (first-class `updatedAt` → atomic writes + local tombstones → Supabase schema + RLS → push tasks/events → pull/reconcile restore → call sync → Settings sync-status screen), then extended to **contacts and deals** (with pipeline-stage merge that never orphans local deals) under a separate opt-in "Contacts & deals" toggle. Schema (`supabase/backup-schema.sql`): RLS policies scope every row to `user_id = auth.uid()`; a DB trigger stamps an authoritative `server_updated_at`; deletes are **soft tombstones**, rows are never hard-deleted. Reconcile (`backup-core.ts`, unit-tested in isolation from Electron) is **id-preserving and non-destructive**: cloud-only → import, local-only → left for push to upload, both-sided → newest wins, and a **genuine two-machine concurrent edit** keeps the losing local copy as a `.conflict` file (surfaced in Settings with "Reveal in Finder") instead of silently discarding it. Also: cloud **scrub** when the privacy toggle is turned off (durable queue drains transcripts/attachment blobs/rows across pushes), Google-event restores no longer duplicate (backup payload carries provider/externalId; only sync _state_ is stripped), and a **clock-skew fix** (see below) so a device with a fast clock can no longer make "newest wins" pick the wrong copy. Hardened by a full-system audit (~35 bugs, `0b090e8..412e984`) plus a same-day follow-up pass on the deferred items. **User action required once:** the SQL in `supabase/backup-schema.sql` (including the later `backup_deals`/`backup_deal_stages`/attachment-bucket additions) must be run in the Supabase SQL editor, or those pushes/scrubs fail best-effort (logged, never blocking core sync).
- **CRM (Phases 1, 3–5; no Phase 2 was ever cut — that number was simply skipped).** ✅ **Phase 1:** Contacts, automatic call↔contact linking, calendar auto-suggest, call-stats/quick-stats polish. **Phase 3:** Deal records with configurable pipeline stages, a pipeline board view, and a deal detail view with linked call history. **Phase 4:** Stale-deal flagging + follow-up-task generation, a needs-follow-up digest, flagging contacts without an open deal, linking tasks to contacts/deals, and a follow-up dashboard of deals needing attention. **Phase 5 (step 1 only):** a manual "Assess risk" action on a deal sends Claude the deal's own metadata plus its contact's _already-computed_ call summaries/coach scores/objection notes (never raw transcripts) for a Low/Medium/High risk verdict, each reason optionally citing a linked call — verified so a reason can never cite a call that isn't actually linked.
- **Knowledge Base + Objection Library.** ✅ A Knowledge Base screen (objection scripts, product info, sales playbook — one JSON file per entry) is assembled into a compact, size-guarded text block that grounds both live cues and post-call coaching in the rep's own material. The **Objection Library** sub-feature is opt-in (off by default): mining reads a call's transcript and proposes objection→response candidates (every quote verified against the transcript) into a human review queue (approve / edit-then-approve / reject); once the toggle is on, new calls auto-mine in the background (never blocking save), and a "Scan my past calls" card handles calls saved before the toggle existed. Consent-purge and duplicate-mining guards added in a follow-up fix.
- **Settings + personalization.** ✅ A dedicated full-screen Settings surface (its own left nav, one page per section) replacing the old inline panel: Account, Recording & consent (master switch), Audio, AI & coaching defaults, Personalization, Privacy & data (honest recap of what stays local), summary language (17 languages + "same as the call"), dark/light/system theme (re-themes existing semantic tokens, no per-component overrides), and the entry points for opt-in cloud sync (M16) and an AI Note Taker.
- **Rebrand + Windows port + noise cancellation.** ✅ Renamed **Sales OS → CallRise AI** (app name/description/window title/docs; `userData` folder, localStorage keys, and the separate virtual-mic device name were **deliberately left unchanged** so existing local calls/tasks/settings aren't orphaned). **Windows port:** macOS-only features (the Core-Audio noise-cancellation card) are guarded off Windows with honest "macOS only" copy, both in the renderer and main process — M12 buyer-side capture was guarded off Windows at this point too, later extended (see M12 above). **Noise cancellation:** integrates the separate `salesos-virtualmic` denoiser repo — a Home-screen card to start/stop it and see live status, with crash-notify/auto-restart and two lifecycle bugs fixed by an independent audit.
- **First-run onboarding.** ✅ A one-time, per-device flow (`features/onboarding/`) shown right after sign-in, before the main app, on a device that hasn't finished (or skipped) it yet — tracked in `localStorage` (`salesos.onboarding.completedAt`), same pattern as every other UI preference, so a new machine simply gets it again. Five steps: Welcome, Who you are, What you sell, Recording (mic-only vs. both sides with consent), and Live coaching cues — each pre-filled from already-saved settings so re-running it never blanks existing values. "Finish" and "Skip" both persist whatever was entered (via the real Personalization/recording/cues stores, not just local state) and mark onboarding done so it never nags again. The final step can drop the user straight onto **Live Calls** instead of Home (`MainApp`'s new `initialNav` prop). Live-tested end to end (all steps, both exit paths, persistence, and the no-repeat guarantee) rather than just code-reviewed.
- **App-wide design system + UX overhaul.** ✅ A ground-up design-consistency pass across every screen, built as a shared primitive library (`components/`: `Badge`, `IconButton`, `Button`, `BackButton`, `StatCard`, `PageHeader`, `SegmentedControl`, `ScoreGauge`, `Skeleton`/`SkeletonRows`, `Modal` — portal-based so it can never be clipped by an ancestor's page-transition animation —, `EmptyState`, `field.ts`) plus new semantic/theme-aware tokens in `index.css` (status `positive`/`warning`/`danger` + `-soft` tints replacing raw Tailwind colors everywhere; a decorative `speaker-1..6` palette for transcript diarization; `track-call`/`track-outlook` calendar-item tokens). Every feature screen was swept to adopt these — status-color tokens, badges, headers, skeleton loaders, and buttons — plus per-screen polish: Home wired to real data with a real recent-calls strip, live-transcript "You"/"Buyer" labels (falling back to "Speaker N" for 3+ participants so distinct people never collapse into one label), `tabular-nums` on data figures, date-bucketed Tasks/Past Calls lists, a pipeline total + AI-risk badges on deal cards, a calendar week-view now-line, a keyboard-shortcuts cheat sheet (`?`), a "+N more" day-peek popover in month view, and **optimistic delete with a 6-second Undo toast** for Tasks/Deals/Past Calls (the real delete only fires if Undo isn't clicked in time; Contacts' delete stays non-optimistic since the "still has open deals" block can only be known by attempting the delete). Verified with a dedicated adversarial regression review of the whole diff (15 confirmed issues fixed — stale Modal focus defaults, a `ScoreGauge`/tier color mismatch, a lost destructive-hover, others) plus live interactive testing in the running app.
- **Call detection + auto-transcribe.** ✅ Notices when a known calling app (WhatsApp, Zoom, Teams, Slack, FaceTime, Skype, MicroSIP, Zoiper, and others — `main/known-calling-apps.ts`) becomes frontmost while the rep is away from CallRise AI, reusing the existing `active-win`-backed polling (`main/active-app.ts`, already built for the auto-start exclusion list — no new dependency, cross-platform Mac+Windows). A top-of-page banner then offers to start transcribing; a new **"Auto-transcribe detected calls"** toggle (Voice AI panel + Settings → AI Note Taker) skips the prompt and starts listening immediately instead. Purely a heuristic nudge — never starts capture without either the explicit banner click or the explicit opt-in toggle.
- **Ambient call detection (multi-signal, native).** ✅ A deeper detection layer than the app-name heuristic above: fuses **CoreAudio (mac) / WASAPI (Windows) mic-session activity, process enumeration, and window-title matching** through a pure FSM/fusion/policy core (`src/main/detection/`: `types.ts`, `fusion.ts`, `stateMachine.ts`, `policy.ts`, `appRegistry.ts`, `CallDetector.ts` — all unit-tested), behind a genuine per-platform native Node addon (`native/mac-audio-activity`, `native/win-audio-sessions`, built via node-gyp targeting **Electron's own Node ABI**, not the system one — a real, previously-hit failure mode where an addon silently never loads in the packaged app). Feature-flagged **off by default** (`detection.enabled` in Settings → a dedicated Detection section). When it fires: a floating always-on-top overlay (banner/toast/switch-prompt/live-capture HUD — `detection-overlay.ts` + `DetectionOverlay.tsx`) and a menu-bar/tray icon (`detection-tray.ts`) offer to start capture, respecting the exact same M11/M12 consent gates as manual capture (no auto-start bypasses consent). Verified via a headless debug CLI (`npm run detect:debug`) plus a dedicated adversarial multi-agent review (57 agents, 43 verified findings, all fixed — stale switch-timer state, unhandled mic-permission errors, tray/overlay stop-button asymmetry, a Settings stale-closure race, others). **Windows-specific:** shipped via a GitHub Actions CI workflow (`.github/workflows/build-windows-demo.yml`) that builds a real, genuinely-portable installer on actual Windows infrastructure (a local-only build only proves the feature works on the machine that built it) — this caught a real bug (`node-gyp.cmd` can't be `execFileSync`'d directly on Windows without `shell: true`) that a Mac-only dev machine could never have surfaced. The overlay was later restyled as a glass HUD capsule (see below). **Critical fix, 2026-07-28:** both adapters' native-addon `require()` had been happening **unconditionally in the constructor**, and an adapter is instantiated at every app startup regardless of the `detection.enabled` setting — so on a Windows machine where the compiled addon can't load cleanly (e.g. a missing VC++ runtime DLL), the whole app could fail to open at all, for every user, even ones who never touched this feature. This is the confirmed real-world cause of "installs fine, then never opens, no error" reported across multiple real Windows testers' machines. Fixed by deferring the `require()` to the first `isSupported()`/`start()` call (both gated behind the feature flag already) — a broken addon can now only ever break detection itself, never the app's ability to launch. **Confirmed fixed, 2026-07-28 (same day):** verified directly on the real Windows PC that had previously failed to launch — after this fix plus a second contributing cause found on the same machine, the packaged app opened successfully. The second cause: `native/win-audio-sessions`'s `binding.gyp` dynamically linked the MSVC C++ runtime (the default `/MD`), so the compiled addon depended on `vcruntime140.dll`/`msvcp140.dll` being present on the machine — missing on a PC that had never had Visual Studio or the VC++ Redistributable installed. Fixed by switching to `/MT` (`RuntimeLibrary: 0`), statically linking the CRT into the addon itself and removing the dependency. Both fixes together are what let the app open on the machine that was failing.
- **CRM Phase 5, further steps.** ✅ Builds on Phase 5 Step 1 (manual risk assessment): deals now keep a **capped history of past risk assessments** (not just the latest), shown as a collapsible timeline on the deal detail view; the follow-up dashboard gained a **`risk-stale`** tier flagging a deal whose risk read is over 30 days old as due for a re-check; and a fresh **High-risk verdict auto-creates a follow-up task** (same shape as the existing manual one, deduped against open tasks).
- **Command palette entity search.** ✅ The existing ⌘K command palette (screens + quick actions + recently-viewed) now does live substring search over **contacts, deals, and calls** by name, deep-linking straight to that record (reusing the `initialViewId` preselect pattern already used by the follow-up digest and AI Note Taker's auto-open) instead of only jumping to a screen.
- **Ambient-detection UI redesign.** ✅ The M15 overlay (banner/toast/switch-prompt/live-capture HUD) was restyled from a plain flat card into a glass HUD capsule — Dynamic-Island/Live-Activity inspired: `backdrop-blur-2xl` translucent surface, a subtle top sheen, pill-shaped buttons, and a breathing live-dot using the `.pulse-ring` utility (defined earlier, unused until now). Pure renderer CSS — identical on Mac and Windows. The tray icon itself still uses one static glyph for every state (tooltip text carries the state instead) — unchanged, since a real per-state glyph needs designed icon assets that don't exist in this repo.
- **M18 — Session health, the audio ring buffer, Windows capture corrected, and the live edge.** ✅ Corrects two wrong diagnoses from an earlier session before building on top of them: **Windows buyer-side capture was never an OS limitation** — it was opening the wrong render endpoint (`eConsole` instead of the `eCommunications` endpoint VoIP apps actually use); and **the "90-second lag" bug is a ratchet, not a spike** — Deepgram's ingest is capped at 1.25x realtime, so a stall costs a multiple of what it stalled, and it never self-heals. **`src/main/session-health/`** (`timeline.ts`, `lag.ts`, `queue.ts`, `liveness.ts`, `drift.ts`) adds a two-cursor lag metric (submitted vs. acknowledged), a bounded-in-seconds audio queue with silence-first eviction, a rising-slope watchdog that catches the ratchet while absolute lag still looks small, a liveness watchdog (capture-dead / socket-dead / silent, three independent clocks because they fail independently), and drift tracking — all reproduced and proven fixed against a real mock Deepgram server enforcing the 1.25x cap (`lag-regression.test.ts`), not against an idealized one. **The audio ring buffer (§1.4, `features/live/audio/ring.ts` + `audio-pump.worker.ts` + `pump.ts`):** the worklet now writes PCM into a `SharedArrayBuffer`; a dedicated Worker drains it on its own thread and streams straight to the main process over a `MessagePortMain`, so a renderer render-storm or long GC pause can no longer queue audio behind it — falls back to the original postMessage path byte-for-byte whenever anything in the chain is missing, and `--diagnose` reports which route audio actually took rather than which one it was supposed to. **Windows capture:** `switch-policy.ts` auto-switches from per-process to whole-device loopback when a live audio meter and total digital silence coincide (the exact shape of both the Microsoft Teams process-loopback bug and the headset-as-communications-device bug), and a zero-native-code mitigation (`buyer-silence.ts`) detects and surfaces the latter with a one-click deep-link to Windows sound settings — the real fix (a per-process-loopback native addon) is designed and documented (`docs/windows-capture.md`) but deliberately **not written blind**: it cannot be compiled, tested, or reach Windows CI while repo pushes are rejected with a 403, and shipping hundreds of lines of unbuildable COM code would only be honestly describable as "unverified." **The live edge (§4, 8 features):** a screen-share-safe glass-HUD cue rail with a two-tier architecture (`useLiveCues.ts`'s `tierFor` keeps every LLM-derived cue off the interrupt path — only the deterministic "pace" cue may interrupt, proven exhaustively over every `CueKind`); deterministic battlecards (32-card starter library, `<0.2ms` p95 match cost, measured); a question-not-asked checklist; a monologue/talk-ratio meter; an instant post-call brief + follow-up email placed on the clipboard from the main process (so it works while the app has no focus); a commitment extractor (who promised what, split rep vs. prospect); plain-English custom trackers ("tell me when someone mentions procurement" → a working trigger, no admin console); and an always-on-time cue-latency instrumentation (`cue-latency.ts`, nearest-rank p50/p95/max). **Security hardening (§5.3):** Electron fuses locked (`RunAsNode` off closes the `ELECTRON_RUN_AS_NODE` code-execution escape hatch that would otherwise inherit the app's own mic grant), CSP shipped as a real response header (not just a `<meta>` tag, which silently drops `frame-ancestors`), a durable consent gate (`consent-gate.ts` — capture cannot start without a consent record on disk, re-sanitized on every write **and** read, 20 unit tests including hand-edited-file attacks), and an auto-updater built to default-deny (no feed configured = no network activity at all, guards against the 2020 Doyensec update-bypass class of bug). Also, mid-milestone: an **"always allow recording the other party"** Settings option (records a real `pre-agreed` consent rather than bypassing one — `sanitizeConsent`'s hard invariant stays intact — off by default since only the rep can know they have standing legal basis); a fixed auto-start latch bug that made every detected call after the first silently do nothing; an earned-not-assumed startup interstitial (no more flash of a mic-permission screen that was never true); and a fix so an **auto-started call now actually ends and saves itself** (ambient detection's end-of-call signal was never wired to the thing that stops capture; the app-name-only detector had no end signal at all, so it now times out after 5 minutes of transcribed silence). **`--diagnose`** (§6) prints one block of text — version, platform, capture path, a channel self-test (a known tone per channel through the real interleaver, catching a rep/buyer channel swap that would otherwise silently mislabel a transcript), live session health, consent state, updater trust, and API-key presence — designed so a tester can paste one block instead of the app needing a live call to debug. **Verification:** `docs/session-health.md`, `docs/windows-capture.md`, `docs/security-audit.md`, and `docs/screen-share-safety.md` each separate what's actually been checked from what still needs a live service, a packaged build, or real hardware this dev environment doesn't have; `docs/acceptance-criteria.md` walks all 13 of the milestone's acceptance criteria with real measured numbers (not assertions) for the 7 fully verifiable in-container, including a `worker_threads`-based proof that a genuinely separate OS thread keeps running through a real 5-second synchronous stall on another thread (248/250 ticks landed inside the blocked window) — and honestly defers the rest (real Windows hardware, a live Supabase project, a live LLM key, two machines for cross-app screen-share verification) rather than guessing. A follow-up adversarial bug-hunt (19 agents) found 12 real issues in the new ring/pump path and elsewhere; 11 were fixed (a worker crash after handshake going silently unhandled, a leaked late-arriving `MessagePort`, stale UI banners that could stack contradictorily, `transcription:captureLost` being emitted but never wired to anything, plus two pre-existing bugs in Tasks/Deals where navigating away during the 6-second undo window silently cancelled the delete instead of completing it). A SEPARATE further-improvements sweep (6 lenses) then caught a real gap in three of the "live edge" features listed above: the monologue meter, commitment extractor, and custom trackers were fully built and unit-tested but had zero callers outside their own tests — no IPC handler, no UI, unreachable by an actual rep. All three are now wired end-to-end: the monologue meter renders next to the engagement gauge in Live Calls; the commitment extractor has a "Find commitments" card on the call detail view (persisted via `Call.commitments`, deliberately excluded from cloud backup until reviewed for what's safe to sync); and custom trackers get a Settings → AI & coaching card (create/list/remove) whose accepted triggers merge into the live `BattlecardMatcher` alongside the starter library.
- **Further-improvements sweep, remaining findings — tests, accessibility, performance, code-splitting.** ✅ Closed the rest of the same sweep's findings (17 in total). **143 new tests** across `backup-core.ts`, `google-sync.ts`, `deals-fs.ts`, `deal-stages.ts`, `coach.ts` (which had `computeMetrics`/`makeVerifier`/`makeFreeTextScrubber`/`normalize` exported specifically so they could be), and `deals/staleness.ts` — turning up two real bugs in the process: `sanitizeDealRecord` never read `stageHistory` back from disk, so the deal-timeline feature silently lost every transition but the most recent one on every reload; and `setDealStages([])` could never actually return its own documented `'empty'` rejection, because the read-path's "never return an empty pipeline" defaulting ran before the emptiness check. **Accessibility:** the live-screen sensitivity picker now reuses the shared `SegmentedControl` (was hand-rolled, no `aria-pressed`, single-letter labels); the Start-transcription button and the Deals board/list toggle + filter chips gained `aria-label`/`aria-pressed` where selection was previously color-only or icon-only with nothing for a screen reader. **Performance:** all seven `list*()` functions across the main-process file stores (calls/tasks/deals/contacts/knowledge/events/objection-queue) read every file in their directory sequentially — switched to `Promise.all` over per-file reads. **Live-screen UX:** a "Jump to latest" affordance once a rep scrolls up mid-call; the "Clip this moment" button gained a z-index so the cue/suggestion rail can no longer paint over it; session health (fully computed, never shown) now overrides the existing latency indicator with a short label ("Reconnecting…", "Catching up…") only when there's an actual problem, kept in its own pure module (`session-health-notice.ts`) specifically so it's unit-testable without dragging in `LiveView.tsx`'s `window`-reading imports. **Onboarding:** the Done screen now checks for a missing Deepgram key and warns before "Start my first call" instead of leading to a silent dead end, and mentions the two off-by-default features (ambient call detection, cloud backup) that onboarding otherwise never surfaces at all. **Code-splitting:** zero existed anywhere in the renderer — `main.tsx` statically imported the entire main `App` AND the ambient-detection overlay into one bundle, and `MainApp.tsx` statically imported all eleven navigable screens despite only ever mounting one at a time. Both are now `React.lazy` + `Suspense`; measured, the main chunk dropped from ~1.43MB to ~558KB, and the always-loaded-but-usually-hidden detection-overlay window in particular no longer pays to parse CRM/Calendar/Coaching/Analytics/Settings/Knowledge/Team code it never uses. Also found and fixed while sanity-checking the packaging pipeline before a build handoff: `apply-fuses.js` guessed the packaged binary's name wrong on Linux (assumed it matched `productName` verbatim, which only happens to hold for macOS and for Windows-because-`electron-builder.yml`-forces-it) — fixed via `packager.executableName`, verified by actually building and running the Linux package end-to-end (AppImage + snap), which previously failed outright at the fuse-flipping step.

- **M21 — HIGH-severity bug fixes + verification.** ✅ (Numbering note: the user's Obsidian vault numbers milestones differently from this file — the vault has M17–M21 where this list stops at M16. "M21" here is the vault's name for this work; the divergence is deliberate and flagged rather than silently reconciled.) Three fixes, two verification answers, one feature, one commit per phase. **Backup clock-skew** was only *half* fixed by M16: the DB stamped an authoritative `server_updated_at` and clamped future-dated `updated_at`, but `reconcileStore` then compared that SERVER timestamp against the local file's **device-clock** `updatedAt` — two different clocks. A device 48h fast made every local record out-rank anything the server had, so a genuinely newer cloud copy was never restored (48h slow inverted it). Fixed with `toServerMs`/`toServerIso` normalising onto the server timeline, skew measured per-sync via a new `server_now()` RPC (midpoint of the request window, so latency isn't misread as skew), normalised uploads so two differently-skewed devices compare correctly server-side, a repair migration that **disables the trigger while it runs** (`set_server_updated_at` returns OLD for a non-newer `updated_at`, and the repair moves it backwards — with the trigger live every repair is silently discarded), and a non-blocking >2min clock warning. Best-effort throughout: unmeasurable skew applies a 0 correction, i.e. exactly the old behaviour. **Coaching attribution** was three compounding defects: (1) `applyConsentRetention` deleted `speaker === 1` from EVERY call, but that constant is a CHANNEL index — on a mic-only call it's a diarization label meaning "the second voice heard", and consent defaults to `not-asked`, so the strip always ran and destroyed the rep's own turns whenever the diarizer labelled them 1 (now gated on a new `buyerCaptureUsed` flag; capture gating untouched, so consent semantics are unchanged); (2) Deepgram restarts diarization per connection, so post-reconnect "speaker 0" is a different person, yet `mergeSegments` merged on the number alone (now a `speakerEpoch` on every result, bumped on connect and on the mono↔multichannel swap, and nothing merges or attributes across one); (3) attribution was re-derived at RENDER time from a mutable whole-call `repSpeaker`, so one change retroactively relabelled the whole call (now an immutable per-segment `role`, back-filled once within its own epoch). Also: `'unknown'` is a real answer rather than a silent default to speaker 0 (shown as "unsure"), the `utteranceEnd` handler takes the speaker whose turn actually ended instead of reading shared state ~1s later, and `assembleReport` prefers the live attribution over re-asking the model. **Verification:** there is **no ~5-second silence cutoff anywhere** — no VAD gate, amplitude threshold, or inactivity timer; `KEEPALIVE_MS = 5000` is the opposite (it stops Deepgram idling out a paused session) and a long pause does not drop a call. The likeliest real teardown with similar timing is the reconnect cascade (~4–6s), which surfaces "Lost connection". Documented at the constant. Windows VoIP buyer capture remains broken (OS/Chromium Communications-role constraint, needs a native Application Loopback Capture addon) and is now documented in `loopback.ts`, whose header previously asserted the path was "genuinely cross-platform". **Mic selection (new feature):** a browser `deviceId` isn't stable — a driver reinstall renumbers it while the name stays — and the capture constraint is a soft `ideal` hint, so a stale id silently recorded from a DIFFERENT mic. Now the label is stored alongside the id, `resolveMic()` repairs a stale id by name, a genuinely absent device is reported instead of swallowed, and the denoising mic is auto-selected while the helper runs but never over an explicit choice. 32 new tests (88 total). **Not verified in a running app** — everything was written on the Windows machine; a macOS build and the platform-gated macOS adapter tests still need the Mac.

- **"Once and for all" bugfix pass (branch `bugfix/once-and-for-all`, worktree).** ✅ Started by merging `origin/claude/windows-build-launch-hsz6x7` (106 commits: M21 Phases B/C/E/F/G, the M22 bug-hunt, M23) into `main` — that work had been sitting complete-but-unmerged; `main` itself had drifted 106 commits behind with only two harmless commits of its own. Then a 6-lens full-flow audit (transcription/audio, diarization/coaching, tasks/calendar sync, backup/auth, consent-flow re-verification, virtual-mic/call-detection) plus two founder-reported bugs found and fixed **10 new Critical/High bugs** (BUG-021–029, BUG-039 in the vault's Bug Tracker), each its own commit with a regression test — for the riskiest six, verified by reverting the fix and confirming the test actually fails first. **Critical:** post-call coaching scorecards came back completely empty for any call recorded with no AI key active live (`isRepSegment` treated `'unknown'` as "confirmed not the rep" instead of falling back to the model's post-call guess); signing out cleared only the Supabase session — every local secret (Google/Outlook OAuth, AI keys) and the account-scoped data itself (calls/tasks/contacts/deals/knowledge/events) stayed fully visible to a different account signing in on the same machine, now blocked by a sign-in-time ownership check (`device-owner.ts`, shared with `backup.ts`'s existing cloud-sync guard) with an explicit, destructive "wipe this device" escape hatch (`device-reset.ts`) for a genuinely different user. **High:** `objection-mining.ts` had its own uncorrected copy of the M21-Phase-G speaker-merge bug; a failed task/event save could silently look successful (write failures were swallowed, never surfaced to the renderer); Outlook's "soonest reminder" picked `Math.min` instead of `Math.max` (fires latest, not soonest — exactly backwards) and had no idempotency guard against duplicate events on an ordinary retry (unlike Google's deterministic-id approach); a stale background app-detection poll could fire a "call detected" broadcast after the rep had already refocused the app, and that same detector ignored the master detection flag and per-app "Never" override entirely; a second `addBookmark` write path could persist the other party's words to the raw on-disk file past a revoked consent, same bug class as an M22 fix in a spot it didn't cover; the Supabase auth-session file was persisted via a fire-and-forget async write with no flush-on-quit hook, so a quit racing Supabase's hourly token rotation could corrupt the saved session; and "Ask the coach"/custom trackers/objection mining/call titles/CRM notes/deal risk all read a single "Default text AI provider" setting whose picker was hardcoded to Claude/ChatGPT only, so a user with e.g. a Groq-only key saw those features fail with a misleading "add your Claude or ChatGPT key" — fixed by showing all 8 `PROVIDER_REGISTRY` providers in the picker and auto-selecting a newly-saved key's provider whenever the current selection has no working key (never overriding an already-working choice). Also added: persistent error/crash logging (`log.ts`, `userData/logs/callrise.log`, reachable from Settings → App), separate from the pre-existing TEMPORARY startup-only crash log. 10 Medium/Low findings logged in `docs/bugfix-once-and-for-all.md` for a future pass, not fixed here (a live-checklist merge-miss, a dormant fast-audio-path safety gap, coach-PDF role display after a reconnect, a floating-overlay dead click when the main window's closed, and others). `AudioRing.hpp` RT-safety (BUG-003) is still blocked — Mac-only, in the `salesos-virtualmic` repo, unreachable from Windows. Full report, per-bug root-cause writeups, a Windows/Mac manual QA checklist, and exact merge commands: `docs/bugfix-once-and-for-all.md`.

**Not yet done:**

- Existing local data (calls/tasks/summaries/events) is **not tied to a user** yet — local files carry no `user.id`. M16's cloud mirror scopes data per-account _in Supabase_ (via RLS), but the local on-disk store itself still isn't stamped; a later milestone may add that (or move local storage into Supabase directly).
- **Google/Outlook→app conflict resolution** (when the same calendar event is edited in both places at once) is still open — both providers push app→Google/Outlook well and pull back read-only, but neither reconciles simultaneous two-sided edits yet.
- **CRM Phase 5** now covers manual risk assessment + history + a stale-check dashboard flag + auto-task-on-High-risk — no further steps have been scoped yet.
- **M12 capture limitations** (deliberate, documented in-app): loopback grabs **all** system audio, so the rep must close other audio and **use headphones** (or the buyer bleeds into the mic and both sides double-transcribe); no per-app/per-window isolation yet; consent is confirmed **mid-call** (no pre-Start consent screen); transcript-only (raw buyer audio is never stored). **Superseded by M18** — see below: Windows buyer-side capture was never actually an OS limitation, it was opening the wrong render endpoint.
- **M18 — Session health, the audio ring buffer, Windows capture corrected, and the live edge.** ✅ Corrects two wrong diagnoses from an earlier session before building on top of them: **Windows buyer-side capture was never an OS limitation** — it was opening the wrong render endpoint (`eConsole` instead of the `eCommunications` endpoint VoIP apps actually use); and **the "90-second lag" bug is a ratchet, not a spike** — Deepgram's ingest is capped at 1.25x realtime, so a stall costs a multiple of what it stalled, and it never self-heals. `src/main/session-health/` (`timeline.ts`, `lag.ts`, `queue.ts`, `liveness.ts`, `drift.ts`) adds a two-cursor lag metric, a bounded audio queue with silence-first eviction, a rising-slope watchdog, a liveness watchdog, and drift tracking — proven against a real mock Deepgram server enforcing the 1.25x cap. **The audio ring buffer** (`features/live/audio/ring.ts` + `audio-pump.worker.ts`): the worklet writes PCM into a `SharedArrayBuffer`; a dedicated Worker drains it and streams to the main process over a `MessagePortMain`, falling back to the original path byte-for-byte if anything's missing. **Windows capture:** `switch-policy.ts` auto-switches per-process to whole-device loopback when a live meter and digital silence coincide; a zero-native-code mitigation (`buyer-silence.ts`) surfaces the remaining gap with a deep-link to Windows sound settings — the real fix (a per-process-loopback native addon) is designed and documented (`docs/windows-capture.md`) but not written blind, since it couldn't be compiled/tested/reach Windows CI from that session's environment. **The live edge:** a two-tier cue architecture (only the deterministic "pace" cue may interrupt), deterministic battlecards, a question-not-asked checklist, a monologue/talk-ratio meter, an instant post-call brief placed on the clipboard, a commitment extractor, plain-English custom trackers, and cue-latency instrumentation. **Security hardening:** Electron fuses locked, CSP as a real header, a durable consent gate re-sanitized on every write and read, an auto-updater built to default-deny. `docs/acceptance-criteria.md` walks all 13 acceptance criteria with real measured numbers where verifiable in-container, honestly deferring what needs real Windows hardware, a live Supabase project, a live LLM key, or two machines for cross-app screen-share verification. A follow-up 19-agent bug-hunt found 12 real issues (11 fixed); a further-improvements sweep then found and wired three "live edge" features (monologue meter, commitment extractor, custom trackers) that were fully built and tested but had zero callers reaching an actual rep.
- **Further-improvements sweep, remaining findings.** ✅ 143 new tests, turning up two real bugs (`sanitizeDealRecord` silently losing deal-timeline history; `setDealStages([])` unable to return its own documented rejection). Accessibility, performance (`Promise.all` over sequential file reads across all seven `list*()` store functions), live-screen UX (jump-to-latest, session-health-aware latency label), onboarding (warns on a missing Deepgram key before "Start my first call"), and code-splitting (main bundle ~1.43MB → ~558KB via `React.lazy`).
- **M21 — HIGH-severity bug fixes + verification.** ✅ (Numbering note: the user's Obsidian vault numbers milestones differently from this file — the vault has M17–M21 where this list stops at M16. "M21" here is the vault's name for this work; the divergence is deliberate and flagged rather than silently reconciled.) Three fixes, two verification answers, one feature, one commit per phase. **Backup clock-skew** was only *half* fixed by M16: the DB stamped an authoritative `server_updated_at` and clamped future-dated `updated_at`, but `reconcileStore` then compared that SERVER timestamp against the local file's **device-clock** `updatedAt` — two different clocks. A device 48h fast made every local record out-rank anything the server had, so a genuinely newer cloud copy was never restored (48h slow inverted it). Fixed with `toServerMs`/`toServerIso` normalising onto the server timeline, skew measured per-sync via a new `server_now()` RPC (midpoint of the request window, so latency isn't misread as skew), normalised uploads so two differently-skewed devices compare correctly server-side, a repair migration that **disables the trigger while it runs** (`set_server_updated_at` returns OLD for a non-newer `updated_at`, and the repair moves it backwards — with the trigger live every repair is silently discarded), and a non-blocking >2min clock warning. Best-effort throughout: unmeasurable skew applies a 0 correction, i.e. exactly the old behaviour. **Coaching attribution** was three compounding defects: (1) `applyConsentRetention` deleted `speaker === 1` from EVERY call, but that constant is a CHANNEL index — on a mic-only call it's a diarization label meaning "the second voice heard", and consent defaults to `not-asked`, so the strip always ran and destroyed the rep's own turns whenever the diarizer labelled them 1 (now gated on a new `buyerCaptureUsed` flag; capture gating untouched, so consent semantics are unchanged); (2) Deepgram restarts diarization per connection, so post-reconnect "speaker 0" is a different person, yet `mergeSegments` merged on the number alone (now a `speakerEpoch` on every result, bumped on connect and on the mono↔multichannel swap, and nothing merges or attributes across one); (3) attribution was re-derived at RENDER time from a mutable whole-call `repSpeaker`, so one change retroactively relabelled the whole call (now an immutable per-segment `role`, back-filled once within its own epoch). Also: `'unknown'` is a real answer rather than a silent default to speaker 0 (shown as "unsure"), the `utteranceEnd` handler takes the speaker whose turn actually ended instead of reading shared state ~1s later, and `assembleReport` prefers the live attribution over re-asking the model. **Verification:** there is **no ~5-second silence cutoff anywhere** — no VAD gate, amplitude threshold, or inactivity timer; `KEEPALIVE_MS = 5000` is the opposite (it stops Deepgram idling out a paused session) and a long pause does not drop a call. The likeliest real teardown with similar timing is the reconnect cascade (~4–6s), which surfaces "Lost connection". Documented at the constant. **Mic selection (new feature):** a browser `deviceId` isn't stable — a driver reinstall renumbers it while the name stays — and the capture constraint is a soft `ideal` hint, so a stale id silently recorded from a DIFFERENT mic. Now the label is stored alongside the id, `resolveMic()` repairs a stale id by name, a genuinely absent device is reported instead of swallowed, and the denoising mic is auto-selected while the helper runs but never over an explicit choice. **Not verified in a running app** — everything was written on the Windows machine; a macOS build and the platform-gated macOS adapter tests still need the Mac.
- **M21 Phase F — fixes from an adversarial review of the M21 diff.** ✅ B/C/E had passing tests and a clean typecheck but had never run in a real app, and they touch consent-gated deletion, backup ordering and the live audio path — so the whole diff went through a 51-agent review (5 dimension reviewers, then 2 refute-by-default verifiers per finding, surviving only if neither could refute it). 23 findings → 13 confirmed, all defects in the M21 work itself. Fixed the critical + all 6 highs (`a9d5d0e`): **(critical)** `importCall` dropped `buyerCaptureUsed`, so ANY restore re-armed the very transcript strip Phase C added it to prevent — the flag is now synced in `callBackupPayload` and preserved from the on-disk record; **(high)** the `buyerCaptureUsed` latch was never reset in `start()`, so every mic-only call after a buyer-capture call in the same session had its speaker-1 turns destroyed; **(high)** the skew-corrected reconcile verdict was vetoed downstream because the store importers re-check `payload.updatedAt` as raw device time — `reconcileStore` now re-stamps the incoming payload onto this device's clock via a new `toDeviceIso`, so the headline "clock 48h forward → restore" scenario actually passes; **(high)** `backup_deal_stages` still compared server vs raw device time; **(high)** settings/deal-stage stamps were double-corrected (a pull stored server time into a field later treated as device time); **(high)** auto-select overrode a deliberate "System default" mic pick, which stores an empty id and was byte-identical to "never chosen". The timestamp family shared one root cause — mixed timelines — so the rule is now explicit: **local is always device time, cloud is always server time, every boundary converts exactly once.** 4 medium findings remain open (identifyRep back-filling label-less turns as the rep; `coach.ts` still merging turns by raw speaker across epochs; the two-instance mic dropdown desync; `CallDetail`'s copied transcript labelling differently from the screen). 98 tests.
- **M21 Phase G — the remaining review mediums.** ✅ Closed the four verified mediums left after Phase F. `identifyRep` was asserting label-less turns as the rep: 'unknown' had two causes (rep not yet identified — real number, back-fillable; vs Deepgram returning no `speaker` field at all — fabricated 0, never back-fillable) and nothing distinguished them, so naming the rep (usually 0) converted every label-less turn into the rep's words; those turns now carry `unlabelled` and stay unattributed. `coach.ts` still applied ONE speaker number across the whole call and merged turns on that number alone — the exact merge `mergeSegments` was changed to reject — so across an epoch a buyer's words could count toward the rep's talk time and be quoted back as evidence for coaching the rep (the comment there explicitly promised that couldn't happen); turn merging and every rep filter now go through `isRepSegment`/`sameTurn`, which prefer the recorded role and never span an epoch. Those helpers plus `repSpeakerFromSegments` moved to **`coach-attribution.ts`** so they are testable at all — `coach.ts` imports Electron and can't load under a plain Node test, the same reason `backup-core.ts` was split out. Also: the two mounted `useAudioDevices` instances desynced (the self-heal writes shared localStorage but only the race winner updated its own state, so the other dropdown showed a device that was no longer being recorded) — fixed with a small subscription; and `CallDetail`'s copied transcript plus `PracticeMode` still labelled speakers the pre-M21 way while the on-screen transcript used the recorded role. 104 tests. **Windows build produced and launch-verified** (installer + portable, both reach a real window); macOS build still needs the Mac.
- **Packaging footgun worth knowing (`da45760`).** A packaged build died on startup with a silent exit 1 — no stderr, no event-log entry. Cause: **asar corruption from a build log redirected into the repo root**. Packaging records each file's size at stat time then streams contents, so a file still being WRITTEN shifts every later offset and `package.json` inside the archive comes back as some other file's bytes; Electron can't parse the manifest and quits before any logging exists. The build reports success and signs the artifact. `electron-builder.yml` now excludes logs/archives/dist/source-maps. **This symptom is indistinguishable from the "installs fine, then never opens" / "This app can't run on your PC" reports from real Windows testers** — unconfirmed as their cause, but a candidate that was never on the list. Debug order for a silent packaged-app exit: (1) `npx electron out/main/index.js`, (2) `npx electron dist/win-unpacked/resources/app.asar`, (3) `npx @electron/asar extract-file app.asar package.json`. If 1 and 2 pass but the `.exe` fails, it's packaging, not code.
- **M21 follow-ups.** `supabase/backup-schema.sql` must be re-run in the Supabase SQL editor (it gained `server_now()` + the clock-skew repair migration); until then skew reads as unmeasurable and the app safely falls back to pre-M21 ordering. `AudioRing.hpp` RT-safety (the macOS side, in the separate `salesos-virtualmic` repo, nowhere on the Windows machine) was **deferred, not attempted** — needs the Mac, or that repo pushed to GitHub. A macOS build and the platform-gated macOS adapter-contract tests also still need the Mac. Four lower-severity issues were logged during the M21 milestone but not fixed: the switch-capture prompt ignores the pending app's own `appOverrides` (so an app set to `never` can still prompt), "Keep current" adds no hysteresis so the same candidate can re-offer ~3s later, there's no "listening but hearing nothing" affordance during silence, and five `DETECTION_TUNING` constants are referenced nowhere.
- **M18 follow-ups.** The Windows per-process-loopback native addon is still design-only (`docs/windows-capture.md`) — that session's repo push access was blocked (403s), so it couldn't reach Windows CI to build/test one; work was delivered as git bundles/patches instead, which is presumably how it reached this branch. **An audible in-call announcement** for the twelve US all-party-consent states (two, MA and PA, carrying criminal penalties) is designed-around but not built — a UI banner is worthless to a remote party who can't see the Electron window; documented as an open gap in `docs/security-audit.md` rather than attempted without discussing the legal/product shape first. A handful of M18 acceptance criteria still need real Windows hardware with a real WhatsApp/Teams call, a live Supabase project, a live LLM key, and two machines for cross-app screen-share verification.
- The Python/FastAPI backend (see Stack, below) still doesn't exist.

## Stack

- **Electron** — desktop shell (main + preload + renderer processes)
- **React 19 + TypeScript** — renderer UI
- **Vite** via **electron-vite** — dev server, hot reload, and bundling
- **Tailwind CSS v4** — styling, via the `@tailwindcss/vite` plugin; theme tokens live in `src/renderer/src/index.css`
- **lucide-react** — icons
- A **Python / FastAPI** backend is planned for **later**. It does **not** exist yet — do not add Python or any backend until we explicitly start that phase.

## Project structure

```
src/
  main/        Electron main process (creates the window)
  preload/     Secure bridge between main and renderer
  renderer/
    src/
      app/         App shell + top-level layout
      features/    One folder per feature (navigation, home, copilot, …)
      components/  Shared, reusable UI primitives (Card, …)
      lib/         Small helpers (cn, …)
      index.css    Tailwind import + dark theme tokens
docs/
  VISION.md    Long-form product vision (owned by the user)
```

## Conventions

- **TypeScript strict mode** is on. Avoid `any`; prefer precise types.
- **Feature-based folders.** New functionality goes in `src/renderer/src/features/<feature>/`. Shared pieces graduate to `components/` or `lib/`.
- **Small, clean commits** — one coherent change per commit, with a clear message.
- **Dark-mode-first UI**, visually inspired by Linear, Raycast, and Arc: calm dark surfaces, clean typography, generous whitespace, soft rounded cards, and a single restrained indigo accent.
- **Path alias:** import renderer code via `@renderer/...`.

## How we work together (standing rules)

- Work in **small, runnable steps** — after each step, the app should still start.
- **Explain in plain language** — the user is newer to coding.
- **Pause and ask for confirmation before big or irreversible changes**: new major dependencies, architectural shifts, deleting things, or anything touching many files. When in doubt, ask instead of guessing.
- ~~Keep scope tight: **no backend, no audio, no AI, no live features** until we plan that work explicitly.~~ **Superseded** — the app now has all four (live transcription, live AI coaching, Supabase auth, Sales Brain memory). The surviving intent: plan substantial new capability explicitly before building it, rather than letting it accrete.
- ~~**Work in the main folder and commit directly to `main`.**~~ **Superseded as of M26/M27 (2026-08).** The founder now works **one worktree per task, branched off `main`, and `main` is never edited directly** — the milestone branches `callrise-m26` / `callrise-m27` and the `fix/1.2.5-consent-leak` hotfix branch all followed this. Field-critical fixes get their **own branch off `main`**, never folded into a milestone branch. Still commit only when the user asks, and **one commit per fix**.
- **Nothing is ever published without the founder saying so explicitly.** Releases are theirs alone, including tagging. Building an installer locally with `--publish=never` to verify is fine; anything that produces a public artifact is not.
- **End with a doc-only CLAUDE.md commit whenever something CLAUDE.md-worthy ships** (a milestone, or a feature substantial enough that a future session would need to know it exists). Do this by default, without being asked — it's what keeps the Progress section from drifting out of sync with the actual commit history, which happened badly once (multiple parallel worktree sessions shipped M15, M16, CRM, Knowledge Base, Settings, and the Windows port with no matching doc update, so the doc silently froze at M14 for ~50 commits).

## Built vs shipped — run this at the START of every session (standing step)

```
node scripts/unshipped-report.mjs
```

**Report the oldest three from section B, every time, unprompted.** A branch over
**three days old either merges or gets a written reason it is waiting** — in the
Bug Tracker entry, not in a chat message that scrolls away.

**Why this is a standing step and not a nicety.** On 2026-09-02 this report was
run once, on a hunch, and found **two serious defects sitting fixed on branches
for seven days while the founder used the app daily**:

- `BUG-111` — pausing a live call for ten seconds ENDED it. A weekly-hit bug.
- `BUG-117` — the app could still ship with **no denoiser**, the feature the
  product is named for, which had already happened to every Windows user once.

Plus a security fix (the raw `ipcRenderer` bridge exposed to the renderer) and
`BUG-114` (Regenerate billing for an AI call and returning the rejected draft).

That is **the exact failure the report was built to catch** — it exists because
BUG-095 was fixed, reported fixed, and then sat unmerged while the founder hit
it daily. It only caught these because someone happened to run it. Running it
by habit is the whole point.

**Two things that make the report lie if you let them.** It cannot see refs that
were never pushed, so run it LOCALLY, not only in CI. And if a fix reaches main
as a **cherry-pick** rather than a branch merge, git does not know the branch is
done and it will keep appearing — after cherry-picking, verify the content is in
main's tree and then **retire the branch**, or the alarm trains you to ignore it.

**"Fixed", "merged" and "shipped" are three different claims**, and merging is
the one most easily spoken as if it were the third.

## How we verify (read this before claiming anything passes)

**The single most valuable document this project has produced is the hollow-green taxonomy** — 15 catalogued ways a test, gate, or guard reports success while proving nothing, each one found live in *this* codebase, plus three closing principles. It lives in the Obsidian vault:

> `03-Features/M26 - The Engine Room (Background Jobs, Progress, Notifications).md` → the **"hollow-green taxonomy"** section.

Read it before writing tests, and re-read it before reporting a milestone green. It is a **checklist to run, not a vaccine you've had** — there is direct evidence in it of the same person relapsing into a species minutes after naming it.

The three rules that generalise past any single species:

1. **Verify your verifier, more than once.** A tool seen passing once is consistent with both "it works" and "it cannot fail." Three separate times, the *verification tooling itself* was the broken thing.
2. **Prove it, don't read it.** Break the thing on purpose and watch the check react. Every conclusion in this project that was instead reasoned from reading code was later found wrong or unverifiable.
3. **Never judge a suite by eyeballing piped output.** A pipe (`cmd | tail`) replaces the exit status; a redirect (`cmd > file 2>&1`) preserves it.

Concretely, for this repo:

- `npm test` — the full suite **through `scripts/run-tests.mjs`**, which streams live, captures everything to `test-output.log`, and propagates vitest's real exit code. Read the exit code, not the summary line.
- `npm run verify:runner` — proves the runner can still *fail*. A runner that can't be shown to fail is a runner nobody should trust.
- `npm run typecheck` — the **project's own** command. Bare `npx tsc --noEmit` at the repo root silently checks **zero files** (the root tsconfig is references-only). It reported "clean" for four entire milestones.
- Every fix gets **red-then-green**: prove the test fails without the fix, *for the right reason*. If a test can't discriminate, say so in the test's own comment and in the report — never count it as proof.

## Common commands

- `npm run dev` — start the app in development (opens the window, hot-reloads on save)
- `npm run build` — typecheck and build for production
- `npm run typecheck` — types only
- `npm run lint` / `npm run format` — lint / format
