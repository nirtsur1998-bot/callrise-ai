# M28 audit findings (2026-08-21)

14-agent adversarial workflow: 8 area finders (file:line evidence required) -> dedupe (59 findings) -> adversarial verification of the top 6 (every one CONFIRMED). Areas: A chat core, B citations, C post-BUG-080 memory, D write actions, E voice, F learning/exclusion, G UX, H accessibility.

## Verified (adversarial pass ran; verdict + decisive evidence recorded)

### V1 [CONFIRMED] Race: excluding during in-flight extraction leaks a memory past the retroactive forget
- **Severity:** high · field-critical: True · area F-learning-exclusion · `src/main/memory/memory-hooks.ts:177`
- **Claim:** runMemoryExtractionForAssistantMessage reads salesBrainExcluded once at start (memory-hooks.ts:177), then spends one AI round trip in extractMemoriesFromChatMessage (extraction.ts:246) plus embedding/AI-judge calls per candidate in consolidateNewCandidate (consolidation.ts:217,225,231) before inserting (consolidation.ts:232), with no re-check anywhere after the gate — so an exclusion set during that multi-second window deletes nothing (the memory doesn't exist yet) and then the insert lands anyway.
- **Failure scenario:** User sends a message; the turn saves and assistant-ipc.ts:265 fires the hook. Hook passes the gate at memory-hooks.ts:177 (not excluded) and awaits the extraction AI call. User — reading the reply, which is exactly when the pill is in front of them — clicks 'Not learning'; assistant-ipc.ts:501 persists the flag and :506-508 deletes every memory tagged assistant:<id> (none from this turn yet). The AI returns; memory-hooks.ts:187 inserts fresh memories from the excluded conversation and :191 even recompiles the profile with them. Pill says 'Not learning', the confirm dialog promised 'will be forgotten', yet the memory persists and feeds retrieval. Same unguarded shape in runMemoryExtractionForCall (:128 check, :135-141 writes) despite its doc comment (:113-118) claiming fresh reads stop it — they only stop a job that hasn't started. Mid-loop exclusion also leaks all remaining candidates (:186-189, no per-candidate re-check). Tests (memory-hooks.assistant.test.ts:91-96) only cover exclusion set before the hook runs. Fix shape: re-read the flag after extraction returns and before each insert (or serialize insert+forget under the conversation lock).
- **Verifier:** Gate read once at memory-hooks.ts:177 with no re-check before inserts (consolidation.ts:232) that follow awaited AI/embedding calls (extraction.ts:246; consolidation.ts:217,225,231); the forget at assistant-ipc.ts:506-508 only deletes memories existing at click time; hook is fire-and-forget (assistant-ipc.ts:265) and holds no lock — withConversationLock (conversations-fs.ts:117) covers only file writes, not the memory DB; rag.ts has no salesBrainExcluded filter so the leaked memory feeds retrieval; tests (memory-hooks.assistant.test.ts:91-96) only cover exclusion set before the hook runs. Severity high not critical: requires clicking exclusion inside the seconds-wide window of one turn, only that turn's memories leak, and a re-toggle purges them.

### V2 [CONFIRMED] First message of a new conversation renders nothing until the turn completes
- **Severity:** high · field-critical: True · area A-chat-core · `src/renderer/src/features/assistant/AssistantView.tsx:460`
- **Claim:** Every conversation started via a starter prompt or by typing with no active conversation shows a blank chat for the whole turn: the send is fire-and-forget (AssistantView.tsx:460, 472-478), the hook's one-time attach() runs before the send starts and before main registers the turn (useAssistantChat.ts:84-118; inFlight.set only happens at assistant-ipc.ts:183 after retrieval+planning), so streamingIdRef stays null and every delta is dropped by the guard at useAssistantChat.ts:121.
- **Failure scenario:** New user clicks the starter prompt 'What do you know about my business?' -> chat pane switches to an empty conversation with no user bubble, no streaming text, no spinner, and no Stop button (chat.sending stays false) for 5-30+ seconds until turnComplete triggers a refetch and both messages pop in at once. If they type a second message meanwhile, they get 'A reply is already in progress — stop it first' with no visible way to stop.
- **Verifier:** Starter path calls raw IPC send, bypassing the hook (AssistantView.tsx:460, comment at :459 documents the wrong assumption), so streamingIdRef/sending are never set by send(); the hook's one-time attach (useAssistantChat.ts:85-88) fires on the setActiveId render, but main only registers the turn at inFlight.set (assistant-ipc.ts:183) AFTER awaited retrieval+planning+lookups (assistant-ipc.ts:145-165, seconds long), so attach returns streaming:false (assistant-ipc.ts:405-410) and the recovery branch (useAssistantChat.ts:92-113) is skipped. Every delta is then dropped by the guard at useAssistantChat.ts:121; Stop button hidden (AssistantView.tsx:710 requires chat.sending); messages appear only via the turnComplete refetch (assistant-ipc.ts:285 → useAssistantChat.ts:130-134). A second message meanwhile gets the 'busy' error (assistant-ipc.ts:147-149) with no Stop control. No renderer test covers this path (only main-side assistant-ipc.turn.test.ts exists). Not critical — the turn is persisted and recovered, no data loss — but every new conversation's first turn renders blank, so high and quickly hit.

### V3 [CONFIRMED] Stop cannot reach the turn during the entire pre-stream phase
- **Severity:** high · field-critical: True · area A-chat-core · `src/main/assistant/assistant-ipc.ts:183`
- **Claim:** The turn is registered in inFlight only at assistant-ipc.ts:183, AFTER awaiting retrieval (up to 10s embed timeout), the plan_research AI call, and lookup execution (lines 158-165); assistant:cancel (lines 397-403) finds no turn and returns false, and the AbortController is created after planning anyway, while planLookups/proposeTask pass no signal at all (tools.ts:106-111, 157-162).
- **Failure scenario:** User sends a message, immediately realizes it's wrong and clicks Stop (visible, since the renderer sets sending=true instantly). During the multi-second planning/retrieval window — up to 45s on a slow provider attempt — cancel is a no-op, the turn proceeds, and the unwanted answer streams and is persisted.
- **Verifier:** Traced end-to-end with no guard. inFlight.set is at src/main/assistant/assistant-ipc.ts:183, after awaited retrieval+planLookups (158-164) and executeLookups (165); it is the only inFlight.set in the file. assistant:cancel (assistant-ipc.ts:397-403) returns false when the turn is absent, and nothing queues the cancel. planLookups (src/main/assistant/tools.ts:104-111) and proposeTask (tools.ts:155-162) pass no AbortSignal; the AbortController is created only at assistant-ipc.ts:178. The renderer sets sending=true before the invoke (src/renderer/src/features/assistant/useAssistantChat.ts:151), AssistantView.tsx:710-712 shows the Stop button whenever sending is true, and stop() (useAssistantChat.ts:192-195) fires cancel once and discards the false return — no retry, no feedback. Tests miss the gap: every cancel test in src/main/assistant/__tests__/assistant-ipc.turn.test.ts:394-444 uses instantly-resolving retrieval/planning mocks, so the 10ms wait lands cancel after registration; the "cancel before any token" test (line 410) is pre-token, not pre-registration. Severity high, not critical: a second Stop click after the first token arrives does work (cancel mid-stream is tested and functional), so the unwanted answer persists only if the user does not retry — but a single Stop during the multi-second planning window is silently swallowed by a visible, enabled control.

### V4 [CONFIRMED] Renderer attaches the wrong memory to citation chips when the model emits any undefined marker
- **Severity:** high · field-critical: True · area B-citations · `C:\Users\User\Desktop\callrise-m28\src\renderer\src\features\assistant\AssistantView.tsx:84`
- **Claim:** CitedText builds its marker-to-index map from EVERY distinct [n] token in the reply (AssistantView.tsx:83-90, regex \[\d{1,2}\]) and indexes into the persisted citations array by position, but main's citationsUsedIn (context.ts:99-105) skips markers the context never defined — so the persisted array and the renderer's index map disagree whenever the model emits an invented/undefined marker, and every chip from that point on is bound to the wrong citation.
- **Failure scenario:** Context defines [1]-[3]. Model replies 'Fake [7] but real [1].' (the exact string in context.test.ts:81). Main persists [cite-for-1]. Renderer maps '[7]'->index 0 and '[1]'->index 1: the fabricated [7] claim gets a tappable chip opening memory 1's evidence modal (wrong memory attached to an ungrounded claim), while the genuinely grounded [1] claim loses its chip and renders as plain '[1]'. Same shift for '[0]' or a leading-zero form like '[01]' (renderer dedupes by string, main by number). Citations have never run against a live retriever, and models routinely emit out-of-range markers, so this inverts the trust feature on real traffic.
- **Verifier:** Renderer and main disagree on citation indexing with no guard between them. Main: context.ts:103-104 only pushes markers found in citationsByMarker (undefined markers consume no array slot; locked in by __tests__/context.test.ts:80-83, which returns a 1-element array for 'Fake [7] but real [1].'). Renderer: AssistantView.tsx:83-90 builds markerToIndex from EVERY distinct [n] string token and binds citations[idx] positionally. Traced concretely: for that exact reply with persisted [cite-1], '[7]' gets index 0 → chip opening memory 1's evidence modal on the fabricated claim, '[1]' gets index 1 → citations[1] undefined → renders as plain text (the grounded claim loses its chip). The renderer receives the persisted array verbatim (useAssistantChat.ts:66-70 refetch of getConversation; assistant-ipc.ts:226,254 persists citationsUsedIn output unmodified). The comment at AssistantView.tsx:104 ('Unmatched markers ... render as-is') only holds when the invented marker first occurs AFTER all real ones; any earlier occurrence shifts every subsequent binding. [0] and [01]-vs-[1] variants also verified (main dedupes by Number at context.ts:100-102, renderer by string). No renderer test for CitedText exists. Severity correction: no data loss/privacy breach and the feature still functions when the model obeys CITE_RULE (context.ts:24), so high (wrong behavior users will hit — misattributed trust chips), not critical.

### V5 [CONFIRMED] Tapping a call citation chip shows a false 'memory no longer available' modal instead of the call
- **Severity:** high · field-critical: True · area B-citations · `C:\Users\User\Desktop\callrise-m28\src\renderer\src\features\assistant\AssistantView.tsx:270`
- **Claim:** EvidenceModal handles only kind==='memory': its useEffect (AssistantView.tsx:269-274) sets evidence to null for any other kind, and the null branch (283-287) renders 'This memory is no longer available — it may have been deleted or Sales Brain is off.' — but search_calls lookup results are persisted as kind:'call' citations (tools.ts:211) and numbered citable exactly like memories (context.ts:75-84), so every call citation chip opens a misleading dead-memory message with no way to open the call, even though citation.id IS the call id and onOpenCall is available in the component.
- **Failure scenario:** User asks 'what did Dana and I discuss last week?'; plan_research dispatches search_calls; the model cites the call result as instructed ('You discussed pricing on the Acme call [6]'). The user taps the chip to verify and is told the memory was deleted or Sales Brain is off — factually wrong on both counts, with the promised 'Open the call' deep link absent. Also fires with Sales Brain OFF (the configuration Rise explicitly supports), where call lookups are the only citations that exist.
- **Verifier:** AssistantView.tsx:270-273 is the only kind check in the assistant renderer: kind!=='memory' sets evidence=null, and the null branch (283-287) renders the "memory no longer available / Sales Brain is off" text. No guard upstream: CitedText renders chips for all citations (88-102), onCite={setCitation} (626), and EvidenceModal opens for any citation (724-726). kind:'call' citations genuinely flow through — created at tools.ts:211, numbered at context.ts:75-84, kept by citationsUsedIn (context.ts:99-105, no filter), and explicitly admitted by the persistence sanitizer at conversations-fs.ts:137 (`c.kind === 'memory' || c.kind === 'call'`). onOpenCall exists in the component (266) but is only used for transcript evidence rows (300-306), never for the call citation itself.

### V6 [CONFIRMED] Save-to-Sales-Brain chips are offered when Sales Brain is off or memory.db failed, and clicking them silently does nothing forever
- **Severity:** high · field-critical: True · area D-write-actions · `src/main/assistant/assistant-ipc.ts:232`
- **Claim:** handleSend extracts and persists memory suggestion chips with no isSalesBrainEnabled() gate (assistant-ipc.ts:232-238, persisted at :249), while assistant:applySuggestion returns {ok:false} when Sales Brain is off or the db is unavailable (assistant-ipc.ts:423-429), and the renderer discards that boolean (AssistantView.tsx:627 calls `void chat.applySuggestion(...)`; useAssistantChat.ts:201 only updates state on ok) — so the chip stays enabled with zero feedback, and each such turn also spends a wasted extraction AI call.
- **Failure scenario:** A user with Sales Brain toggled off — or on one of the clean-Windows machines where better-sqlite3/sqlite-vec fails to load (the exact failure class the M28 doc says Rise must survive) — chats with Rise, gets a durable 'Save to Sales Brain' chip persisted on their message, clicks it, and nothing happens: no 'Saved', no error, chip stays clickable on every reload, and nothing is ever written.
- **Verifier:** assistant-ipc.ts:232-238 extracts memory chips with no isSalesBrainEnabled() gate and persists them at :249; applySuggestion returns bare {ok:false} when brain is off or db is null (assistant-ipc.ts:423-429, memory-runtime.ts:197); renderer discards it (AssistantView.tsx:627 `void chat.applySuggestion`, useAssistantChat.ts:201 updates only on ok, no error set), and the chip (AssistantView.tsx:232-249) is disabled only via appliedSuggestionIds, with sanitizeSuggestions (conversations-fs.ts:144/:220) never stripping chips on read — so it stays clickable forever. No guard exists in extractContextSuggestions (coaching-chat.ts:246-306) or renderer (zero salesBrain hits in AssistantView.tsx); purpose-health-store.ts:182 gates only 'memory-*' purposes, so the 'coaching-chat' extraction call really is spent each turn. docs/M28-rise.md:21-24 explicitly requires Rise to work with Sales Brain OFF and names the sqlite failure class. Fails closed (no write, no privacy breach), so high rather than critical.

## Unverified area findings (no adversarial pass; treat as leads with evidence)

- **[medium] A-chat-core** — Busy-check TOCTOU lets two turns run concurrently in one conversation, corrupting in-flight state (`src/main/assistant/assistant-ipc.ts:147`). The inFlight.has busy check (assistant-ipc.ts:147) and inFlight.set (line 183) are separated by three awaits (retrieval, planning, lookups), so a second send in that window passes the check; the second set() overwrites the first turn's entry and the first turn's finally (line 279) deletes the SECOND turn's entry, breaking cancel/attach for the still-streaming turn, while both turns' assistant:delta broadcasts append into the renderer's single streaming bubble (useAssistantChat.ts:120-125 filters only by conversationId). *Scenario:* Because the composer stays enabled during a fire-and-forget first turn (finding 1, chat.sending=false), the user retypes and sends while the first turn is still planning: two turns stream concurrently, interleaved text appears in one bubble, Stop returns false for the surviving turn, and attach reports not-streaming while tokens are still arriving.
- **[medium] A-chat-core** — Model-invented citation marker shifts the renderer's marker-to-citation mapping onto wrong evidence (`src/renderer/src/features/assistant/AssistantView.tsx:84`). Main builds the persisted citations list from DEFINED markers only, in first-use order (context.ts:97-106 skips markers not in citationsByMarker), but CitedText indexes ALL distinct [n] markers in first-occurrence order (AssistantView.tsx:82-90) and maps index i to citations[i], so one invented marker appearing before a real one steals the real marker's citation and pushes the real marker off the end of the array. *Scenario:* Model writes 'Their budget is tight [9] and they prefer email [2]' where [9] was never defined and [2] is real: main persists citations=[cite-for-2]; the renderer renders [9] as a tappable chip whose evidence modal shows the memory backing [2] (wrong claim gets the evidence), and [2] renders as dead plain text — the trust/traceability feature actively misleads.
- **[medium] A-chat-core** — No total prompt bound: 40-message history with 16k-char messages plus profiles/sections is never tokenized or truncated (`src/main/assistant/assistant-ipc.ts:173`). The only bounds are per-piece char caps (MAX_INBOUND_CHARS 8000 at assistant-ipc.ts:60, MAX_MESSAGE_TEXT 16000 at conversations-fs.ts:98, MAX_HISTORY_MESSAGES 40 at assistant-ipc.ts:59); the assembled prompt (full profiles + 5 memories + up to 3 lookup sections + 40 messages, lines 166-189) can reach ~300k+ chars and streamWithFallback passes messages through with no truncation or token counting (complete-with-fallback.ts:899 onward), so oversize prompts surface as opaque provider errors and fallback-chain churn. *Scenario:* A user who pastes large text (near the 8k cap) each turn accumulates 40 x ~8-16k chars of history; by mid-conversation every turn exceeds smaller models' context windows, each chain entry 400s in sequence, and the user sees 'Every model failed' (or a long fallback walk) with no hint the conversation length is the cause.
- **[medium] A-chat-core** — listConversations fully parses every conversation file and the renderer triggers it at least twice per turn (`src/main/assistant/conversations-fs.ts:271`). listConversations (conversations-fs.ts:271-295) sequentially reads and JSON.parses every .json file in full — up to 500 messages x 16k chars (~8MB) each — just to emit id/title/count/preview, and AssistantView calls it on mount, after every send (AssistantView.tsx:481), and on EVERY assistant:turnComplete broadcast regardless of conversation or window (AssistantView.tsx:433-439), with the broadcast itself going to all windows (assistant-ipc.ts:72-76, 285). *Scenario:* With 50+ conversations several of which are long, every completed turn causes >=2 full scans of every conversation file on the main process (JSON.parse of megabytes blocks the main event loop), multiplied per open window — list refresh and the whole app get progressively janky exactly as usage grows.
- **[medium] A-chat-core** — Exclusion race: 'don't learn from this conversation' cannot stop or undo an in-flight extraction (`src/main/assistant/assistant-ipc.ts:264`). handleSend fires runMemoryExtractionForAssistantMessage fire-and-forget (assistant-ipc.ts:264-268); the hook checks salesBrainExcluded once before its AI call and writes memories after it (memory-hooks.ts:169-192), while assistant:setSalesBrainExcluded deletes only memories that exist at toggle time (assistant-ipc.ts:503-510) — so an extraction in flight when the user excludes the conversation lands its writes AFTER the retroactive delete, breaking the documented 'exclusion leaves zero trace' guarantee. *Scenario:* User sends a sensitive message, immediately clicks 'Learning' -> confirms 'Stop learning... will be forgotten': the delete loop runs against existing memories, then the extraction for that very message (a multi-second AI call already past its permission check) completes and writes new memories tagged assistant:<conversationId> that persist despite the exclusion.
- **[medium] A-chat-core** — Retroactive forget silently skipped when memory db is unavailable, while reporting success (`src/main/assistant/assistant-ipc.ts:504`). assistant:setSalesBrainExcluded uses only getMemoryDb() with no ensureMemoryDb retry (assistant-ipc.ts:504-509, unlike applySuggestion's retry at line 428): if startup init failed (the documented better-sqlite3/sqlite-vec clean-machine failure class), the memory-deletion loop is skipped entirely yet the handler still returns ok:true and the UI shows 'Not learning' as if the forget happened. *Scenario:* On a machine where the native module failed at startup but the db file is intact, the user excludes a conversation and is told it's forgotten; after a restart where init succeeds, the conversation's memories are back in retrieval and Memory Center — a consent promise ('forgotten, cannot be undone') that silently did nothing.
- **[medium] A-chat-core** — Failed send leaves a phantom user bubble not on disk and orphans the voice-note file (`src/renderer/src/features/assistant/useAssistantChat.ts:185`). On ai-failed the hook removes only the streaming bubble and keeps the optimistic user bubble (useAssistantChat.ts:185-187) even though appendTurn never ran, so the message exists only in component state (the exact BUG-048 shape); additionally AssistantView clears the pending voice note before sending (AssistantView.tsx:470-471) and the failure path never calls discardVoiceNote, orphaning the already-saved audio file in assistant-conversations/media. *Scenario:* AI providers are down (routine on free-tier keys): user sends a dictated message, gets the error banner, navigates to Live Calls and back — their message has vanished from the conversation (refetch reads disk), the dictation text is gone, and the recorded audio sits on disk unreferenced forever.
- **[low] A-chat-core** — Recovered stream can display garbled text: deltas between attach snapshot and renderer state-set are dropped mid-string (`src/renderer/src/features/assistant/useAssistantChat.ts:92`). On re-attach, deltas broadcast after main captured snapshot.accumulated but before the renderer sets streamingIdRef (useAssistantChat.ts:92-113 runs async after the attach round-trip) are discarded by the guard at line 121, so subsequent deltas append to a snapshot missing a middle chunk; the text is only corrected by the turnComplete refetch. *Scenario:* User navigates back mid-answer during fast token flow: the recovered bubble reads 'the deal closed at $40...thousand' with a chunk missing at the splice point, visibly garbled until the turn completes and the refetch repaints the correct persisted text.
- **[medium] B-citations** — Deltas emitted during attach-recovery are silently dropped, leaving a mid-text gap until the turn completes (`C:\Users\User\Desktop\callrise-m28\src\renderer\src\features\assistant\useAssistantChat.ts:121`). On re-attach, the onDelta handler discards deltas while streamingIdRef is null (useAssistantChat.ts:120-124), and streamingIdRef is only set after both getConversation and attach IPC calls resolve (85-113) — so any delta broadcast after main captured the attach snapshot but before the renderer applied it is lost from the displayed text (later deltas append to the stale snapshot), producing a reply with missing words mid-sentence, possibly splitting a [n] marker, until the turnComplete refetch replaces it with the persisted truth. *Scenario:* User navigates away mid-answer and back while the model is streaming fast. The recovered bubble shows snapshot text plus post-recovery deltas with a chunk missing in between — e.g. 'Dana prefers annual bil[GAP]he decision maker [2]' — for the rest of the stream. Self-heals when the turn settles, so the damage is transient and confined to the recovery path.
- **[high] C-post-bug080** — KNN applies k BEFORE scope/status filters — retrieval re-degrades toward empty at real scale (`src/main/memory/memories-store.ts:203`). searchMemoriesByVector's SQL (`WHERE v.embedding MATCH @embedding AND k = @limit` then `AND m.status IN (...) AND m.scope = @scope`, memories-store.ts:197-214) runs the KNN inside the bare vec0 table (migrations/index.ts:53 — no partition key or metadata columns), so sqlite-vec selects the k globally-nearest vectors across ALL scopes and ALL statuses first, and the scope/status predicates only post-filter those k rows; since rag.ts:105-110 calls it per-scope with the same embedding and same k=5, the union across rep/business/client is at most the SAME global top-5, meaning any in-scope relevant memory ranked 6th+ globally is unreachable, and archived/invalidated/other-client memories silently consume the 5 slots. *Scenario:* A user with months of data (e.g. 300 rep memories, 40 business, several client scopes, plus decay-archived rows — decayMemories at consolidation.ts:427-431 archives without deleting vec rows) asks a business question; the 5 globally-nearest vectors are rep-scope or archived, the post-filter discards them all, and retrieval returns 0-1 results despite relevant business memories existing just past global rank 5 — the exact empty-retrieval symptom BUG-080 fixed, reintroduced by a different mechanism. Also weakens consolidation's dedupe pre-filter (consolidation.ts:218-222, global k=3 post-filtered by scope). Invisible to the 24-row eval corpus and to the ≤2-row unit tests (memories-store.test.ts:113-144); the baseline doc's own q-acme-tool miss ('outranked by business-scope memories') is this bias already showing at 24 rows.
- **[high] C-post-bug080** — Coaching chat still awaits retrieval inline with no foreground protections — the M28 fix was wired only into Rise (`src/main/coaching-chat-ipc.ts:159`). coaching-chat-ipc.ts:157-159 awaits retrieveRelevantMemories(message, contactId) inline in handleSend with no timeout wrapper, and rag.ts:122-126 shows that wrapper passes only { contactId } — never foreground:true — so the 10s embed bound and ensureMemoryDb retry built in M28 (rag.ts:44, :56-60, :87-95) protect only Rise (assistant-ipc.ts:158-162); the coaching chat, the very surface M27 flagged for this un-timeouted await, keeps the unbounded background path (rag.ts:95 awaits embedText with no race). *Scenario:* Fresh install (or purged memory-model-cache) with Sales Brain on: the rep opens a call's coaching chat while the fire-and-forget warmUpEmbeddings download is still running or hasn't started; the chat turn blocks on the ~23MB model download — up to ~48s per embeddings.ts:77-81's own Phase-0 measurement — with no spinner explanation and no fallback, on the exact reply the rep is watching. Also no ensureMemoryDb retry: a lost 15s startup-init race leaves coaching-chat retrieval silently empty all session (memory-runtime.ts:151-162) while Rise recovers.
- **[high] C-post-bug080** — rag.ts's 'never throws' contract is false: embedText rejection fails the whole turn (and Rise's assistant:send has no catch at all) (`src/main/memory/rag.ts:93`). retrieveRelevantMemoriesStructured documents 'never throws' (rag.ts:78-80) but has no try/catch around embedText: on the background path (rag.ts:95) a rejection propagates directly, and on the foreground path withTimeout uses Promise.race (rag.ts:63-75) which resolves null only on TIMEOUT — a fast rejection (model download fails: offline, blocked CDN) rejects through. The rejected extractor promise stays memoized for the session (embeddings.ts:64-71; only warmUpEmbeddings clears it, :94-103 — embedText callers never do). coachChat:send's catch (coaching-chat-ipc.ts:328-332) turns this into 'Something went wrong' failing the ENTIRE turn instead of the designed memory-blind degradation, and assistant:send (assistant-ipc.ts:325-339) has no try/catch, so Rise's renderer invoke() rejects with a raw error. *Scenario:* Machine with Sales Brain on and no cached embedding model, offline or with huggingface.co blocked (corporate proxy): warmup fails and clears; the first chat turn re-attempts the download, embedText rejects, and every coaching-chat and Rise turn for the rest of the session errors out entirely — chat unusable — even though profiles and the provider chain are fine and the code's stated intent everywhere is 'turn proceeds memory-blind'.
- **[high] C-post-bug080** — Dedupe threshold 0.35 carries the same cosine-vs-L2 unit bug as BUG-080 — paraphrase merge tier is unreachable, duplicates now surface in retrieval (`src/main/memory/consolidation.ts:38`). DUPLICATE_VECTOR_DISTANCE_THRESHOLD = 0.35 is commented 'cosine-ish distance… clearly related territory, not just same broad topic' (consolidation.ts:38), but against vec0's L2 distances on unit vectors 0.35 demands ~94% cosine similarity — near-verbatim — while rag.ts:29-31 and M28-retrieval-baseline.md:18-20 measured that differently-worded restatements land at L2 1.0-1.25 (cos ~0.2-0.5). So the judgeSameFact AI tier, explicitly built for 'DIFFERENTLY-WORDED statements' (consolidation.ts:42-49, :57-58) behind this pre-filter (:218-222), essentially never fires: paraphrased restatements skip both the exact-match check (:209-211) and the vector tier and insert as fresh memories. rag.ts:34-36 declares 0.35 'deliberately unchanged' but justifies it for the exact-identity case, not the paraphrase-judge tier the threshold actually gates. *Scenario:* A fact restated in different words across 3 calls ('pricing is $49/seat', 'we charge forty-nine per seat monthly') creates 3 separate memories instead of one reinforced memory: distinctEpisodeCount stays 1 per row so the 3-episode promotion (consolidation.ts:257, :264-271) never triggers — the fact stays hypothesis forever, invisible to coaching-chat retrieval (active-only) and to compiled profiles (active-only, :469) — and post-BUG-080 a Rise question about pricing can fill several of its 5 retrieval slots (rag.ts:110) with near-duplicates of one fact, crowding out distinct relevant memories.
- **[medium · field] C-post-bug080** — Rise never passes contactId, so client-scope memories are unreachable — while the eval's 'Rise config' measures WITH contactId, masking it (`src/main/assistant/assistant-ipc.ts:159`). Rise's retrieval call (assistant-ipc.ts:158-162) passes only foreground+includeHypotheses — no contactId — so rag.ts:105 searches only rep+business scopes and every client:* memory is structurally invisible to Rise; yet the retrieval eval's 'DEFAULT active+hypotheses (Rise)' configuration (retrieval-quality-eval.test.ts:92-97, :167-168) feeds each question's contactId through, so 7 of its 14 questions (all q-acme-*/q-globex-*, corpus:102-107) — including the hypothesis-gated headline win q-acme-expansion — measure a call shape Rise never makes; real Rise recall on client questions is 0 by construction. *Scenario:* User asks Rise 'who makes the buying decisions at Acme?' — the memory 'Decision maker is Dana Levy' sits in client:acme scope, visible in Memory Center, but retrieval cannot return it (find_contact lookups return CRM fields, not memories); Rise says it doesn't know — the exact 'I don't know next to a visibly full Memory Center' credibility trap docs/M28-rise.md:54-58 says the includeHypotheses work exists to prevent. Contact binding is a documented Phase 2+ deferral (M28-rise.md:115-117), but the eval's 93%-recall 'Rise' label reports a configuration that isn't the shipped one.
- **[low] C-post-bug080** — Retrieval eval measures no precision, no scale, and a duplicate-free corpus — its blind spots align with the real failure modes (`src/main/memory/__tests__/retrieval-quality-eval.test.ts:143`). The report scores only recall@5, MRR, shouldNotSurface violations, and empty answers (retrieval-quality-eval.test.ts:115-146): distractors surfacing on unrelated questions cost nothing (only q-scope-isolation defines shouldNotSurface, corpus:107; the four d-* distractors are otherwise unscored, so 'distractors that should stay quiet' at corpus:12 is asserted, not measured); the corpus is 24 rows (corpus:47-77), far too small to expose memories-store.ts:203's k-before-filter bias; and it is seeded via insertMemory directly (eval:65-69), bypassing consolidateNewCandidate, so the paraphrase-duplicate-laden state of a real DB is never modeled; no question tests cross-scope ranking when two scopes both hold relevant answers. *Scenario:* The three highest-impact retrieval defects found in this audit (scope starvation at scale, duplicate crowding of the top-5, and precision loss from mid-list distractors — the last already visible in M28-retrieval-baseline.md:67-68 but unscored) all pass this harness green; a Phase 2 'improvement' could regress any of them without moving the reported numbers.
- **[low] C-post-bug080** — Post-BUG-080 prompts carry each top memory twice: once in the full profile, once as a numbered citation (`src/main/coaching-chat-ipc.ts:158`). Both consumers inject the full compiled profiles AND question-scoped retrieval (coaching-chat-ipc.ts:157-159; assistant-ipc.ts:166-171); profiles are built from the same active memories ranked importance×confidence up to 4200 chars (consolidation.ts:437-466), so on a typical install the profile contains all active memories and every retrieved active memory now appears verbatim twice in one prompt — before BUG-080 the retrieval half was always empty, so the duplication never existed; in Rise, the profile copy is un-numbered while the retrieved copy is [n]-numbered (context.ts:50-71), giving the model a legitimate uncited source for every claim the CITE_RULE (context.ts:23-24) wants cited. *Scenario:* Rise answers 'you tend to run discovery long' drawing on the profile line and omits the [n] marker despite the identical memory being numbered in the same prompt — the answer is right but the tappable-citation trust surface silently under-reports its evidence; plus redundant tokens on every turn.
- **[medium] D-write-actions** — Failed confirmTask gives no user feedback — 'Add task' click silently does nothing when the conversation was deleted or task creation fails (`src/renderer/src/features/assistant/AssistantView.tsx:628`). assistant:confirmTask returns {ok:false} on every failure path — conversation file deleted before accept (conversations-fs.ts:393-394), proposal already accepted, or createTask throwing with successful revert (assistant-ipc.ts:486-489) — but AssistantView.tsx:628-630 discards chat.confirmTask's boolean and useAssistantChat.ts:222-236 only mutates state on res.ok, so the user sees the chip stay 'Add task' with no error message anywhere. *Scenario:* User deletes the conversation from the rail in a second window (or task-dir write fails, e.g. disk full), then clicks 'Add task' on a still-rendered chip: the click returns false, no task is created, no error banner or toast appears, and the button just sits there as if the click never happened — the user reasonably believes the task was added or the app is broken.
- **[medium] D-write-actions** — Chip can permanently show 'Added' with no task ever created: accepted status is persisted before createTask, and the revert path is itself unguarded (`src/main/assistant/assistant-ipc.ts:486`). confirmTask persists status:'accepted' to disk before the task exists (acceptTaskProposal writes at conversations-fs.ts:398-400, createTask runs after at assistant-ipc.ts:478-483); if createTask fails AND the compensating revertTaskProposal write also fails (its writeConversation at conversations-fs.ts:438 is not wrapped, and the `await revertTaskProposal` inside the catch at assistant-ipc.ts:487 has no try around it, so the IPC handler rejects), or the app quits/crashes in the accept→create window, disk keeps 'accepted' while no task was written. *Scenario:* createTask throws (tasks dir on a full/locked disk) and the revert write to the conversation file also throws, or the app is killed between the accept write and createTask: on next launch the chip renders 'Added ✓' (sanitizeTaskProposals preserves status at conversations-fs.ts:200), the task list contains nothing, and because accepted proposals are refused forever (conversations-fs.ts:397) the user has no way to retry — a confirmed task silently never exists.
- **[low] D-write-actions** — applySuggestion has no idempotence guard and ignores markSuggestionApplied's failure — memory written but chip re-offered, or ok:true when the applied marker was never persisted (`src/main/assistant/assistant-ipc.ts:448`). Unlike confirmTask's accepted-status guard, assistant:applySuggestion never checks appliedSuggestionIds before writing (assistant-ipc.ts:412-449), and it discards markSuggestionApplied's null return (returns {ok:true} at :449 even when the conversation/message lookup failed at conversations-fs.ts:452-453); conversely if markSuggestionApplied throws after consolidateNewCandidate succeeded, the outer catch returns {ok:false} with the memory already written. *Scenario:* Rapid double-click or a second window applies the same chip twice: consolidateNewCandidate runs twice (contained to a reinforce by the exact-match dedupe at consolidation.ts:208-215, but it still appends a duplicate evidence entry via reinforceMemory). Or: consolidate succeeds, the mark write fails — the UI keeps the chip active ('not saved') while the memory is in the Sales Brain, and after reload the chip is re-offered and re-applicable.
- **[low] D-write-actions** — Second window keeps stale actionable chips after confirm/apply — no broadcast or cross-window refresh for write actions (`src/main/assistant/assistant-ipc.ts:490`). assistant:confirmTask and assistant:applySuggestion update only the invoking renderer's local state (useAssistantChat.ts:222-236, :201-212); unlike sends, which broadcast assistant:turnComplete to all windows (assistant-ipc.ts:285), no event is emitted after a confirm/apply, so any other window showing the same conversation keeps a pending 'Add task' / 'Save to Sales Brain' chip until it re-navigates. *Scenario:* With the app open in two windows on the same conversation, the user confirms the task in window A; window B still shows 'Add task', and clicking it there returns a silent false (the accepted guard refuses it) — combined with the no-feedback finding, window B's chip looks permanently broken even though the task exists.
- **[low] D-write-actions** — applySuggestion trusts the renderer-supplied suggestion payload instead of validating it against the persisted message (`src/main/assistant/assistant-ipc.ts:419`). The handler writes whatever suggestion object the renderer sends — memoryScope/memoryCategory/text are cast unchecked into consolidateNewCandidate with confidence 0.95 and source 'user_stated' (immediately trusted, per the coaching-chat precedent at coaching-chat-ipc.ts:372-390) — without confirming the suggestion id/content exists in the persisted message.suggestions, and conversationId is only typeof-checked (assistant-ipc.ts:421), not isSafeConversationId-validated, before being embedded into the evidence callId `assistant:<conversationId>` (:439). *Scenario:* Any renderer-side bug (or compromised renderer) that mutates the suggestion object before the IPC call writes an arbitrary statement into an arbitrary scope as an immediately-active trusted memory, with an evidence callId that can contain arbitrary characters — bypassing the persisted-chip contract that main itself wrote to disk one call earlier.
- **[high] E-voice** — 5-minute cap discards the whole recording and wedges the composer (`src/renderer/src/features/assistant/useVoiceNote.ts:95`). The auto-stop at MAX_RECORDING_MS calls recorder.stop() with stopResolveRef unset and performs no teardown or state transition, so the entire recording is dropped in onstop (line 84's resolver is null) and the hook stays in 'recording' with the timer ticking past 5:00; a later 'Done' finds the recorder already inactive, so its stop() produces no onstop and the blobPromise at lines 124-130 never resolves, leaving the UI stuck on 'Transcribing…' forever (or, on engines that throw InvalidStateError for stop() on an inactive recorder, an unhandled rejection with the UI stuck on the recording bar). *Scenario:* User dictates for 5 minutes; at 5:00 the recorder silently stops (all audio discarded via the null resolver), the red recording bar keeps counting up, and pressing Done switches to a permanent disabled 'Transcribing…' button — the 5 minutes of speech are irrecoverable and voice notes are dead until the view remounts.
- **[high · field] E-voice** — 'Your recording is kept' error copy is false — transcription failure loses the audio irrecoverably (`src/main/assistant/voice-note.ts:81`). The api-failure message promises 'Your recording is kept — try again or type instead' (and assistant-ipc.ts:343-344 claims 'the renderer keeps its local blob'), but in useVoiceNote.ts stopAndTranscribe the blob is a local const: on !result.ok it only setError and returns '' (lines 142-144), the blob is garbage-collected, and no retry path exists (stopAndTranscribe requires state==='recording'; chunksRef is never reused). *Scenario:* Deepgram returns 500 (or the network drops) after a long dictation; the composer shows 'Your recording is kept — try again or type instead', but there is nothing to retry — the audio is gone and the user must re-record from scratch. Also hits every user without a Deepgram key on their first voice note.
- **[high] E-voice** — Mic unplugged mid-record: no onerror/track-ended handling, recording lost and UI stuck (`src/renderer/src/features/assistant/useVoiceNote.ts:79`). The recorder wires only ondataavailable and onstop (lines 79-86) — no recorder.onerror and no 'ended' listener on the stream's tracks — so a device disconnect stops the MediaRecorder on its own, onstop fires with stopResolveRef null and discards every chunk already captured, and the hook remains in 'recording' with the timer counting on a dead stream. *Scenario:* User records with a USB/Bluetooth headset that disconnects mid-note: the UI keeps showing the live recording bar (level flat), and pressing Done hangs at 'Transcribing…' forever because the inactive recorder never fires another onstop to resolve blobPromise — everything spoken before the disconnect is lost too.
- **[medium] E-voice** — Send failure silently drops the attached voice note and orphans its media file (`src/renderer/src/features/assistant/AssistantView.tsx:471`). handleSend calls voice.clearPending() (which does not delete the file) before chat.send resolves; if the send fails (ai-failed/busy/not-found — useAssistantChat.ts:184-187 just shows the error), the message is never persisted, so the saved audio at mediaId is orphaned on disk forever and the pending chip is already gone, leaving the user no way to discard or re-attach the note. *Scenario:* Provider is down: user records a note, reviews the transcript, hits Send; the error banner appears, the draft is not restored, the 'Voice note attached' chip has vanished, and the webm file sits in conversations/media with no message referencing it and no cleanup path.
- **[medium] E-voice** — New-chat and starter-prompt paths handle the pending voice note incoherently (`src/renderer/src/features/assistant/AssistantView.tsx:473`). Sending on a fresh chat (activeId null) deliberately discards the audio (lines 473-475) so the persisted first message silently has no playback chip; meanwhile the 'New chat' button (lines 515-518) clears the draft but not voice.pending, and the starter-prompt buttons (line 565) bypass handleSend entirely, so a pending note recorded on the empty screen survives and attaches to a later, unrelated message in the new conversation. *Scenario:* (a) A user's first-ever voice note — recorded on the empty new-chat screen — sends its transcript but the audio is silently deleted, so the expected playback chip never appears. (b) User records a note, clicks 'New chat': the transcript vanishes from the composer but the 'Voice note attached · 0:42 — review the text below' chip remains pointing at nothing; their next typed message in an existing conversation ships with an audio attachment that says something completely different.
- **[medium] E-voice** — Double-click on the mic starts two concurrent recorders sharing one chunk array (`src/renderer/src/features/assistant/useVoiceNote.ts:66`). start()'s guard (state !== 'idle', line 66) is defeated by the async gap: two rapid clicks both see 'idle' before setState('recording') at line 90 commits, so two getUserMedia streams and two MediaRecorders are created; the second overwrites streamRef/recorderRef so the first stream's tracks are never stopped by teardown (line 55), and both recorders' ondataavailable handlers push into the same chunksRef.current array (line 80), interleaving two independent opus streams into one corrupt blob. *Scenario:* User double-clicks the mic button: the first recorder keeps capturing forever (mic-in-use indicator never clears, chunks accumulate unbounded), and stopping produces a blob with interleaved chunks from two containers — playback/transcription of a corrupt webm.
- **[medium] E-voice** — stopAndTranscribe has no catch around the IPC call — a rejection freezes 'Transcribing…' permanently (`src/renderer/src/features/assistant/useVoiceNote.ts:136`). The await of window.api.assistant.transcribeVoiceNote (lines 136-140) is uncaught; the main handler calls saveVoiceNote outside any try/catch (assistant-ipc.ts:373), so a disk-write failure rejects the invoke, setState('idle') at line 141 never runs, and the void'd handleMicStop (AssistantView.tsx:484) swallows the rejection while the composer shows the disabled 'Transcribing…' button until remount. *Scenario:* Disk full or media dir unwritable when the user finishes a recording: transcription succeeded upstream but saveVoiceNote throws, the IPC promise rejects, and the composer is stuck on a disabled 'Transcribing…' button — no error shown, recording lost, mic unusable until the user navigates away and back.
- **[medium] E-voice** — deleteVoiceNote lacks the media-id safety check readVoiceNote has — renderer-driven path-traversal unlink (`src/main/assistant/voice-note.ts:136`). deleteVoiceNote validates only the extension (/\.(webm|ogg)$/) and never runs isSafeMediaId, unlike readVoiceNote (line 125), and the assistant:discardVoiceNote IPC handler (assistant-ipc.ts:384-386) forwards any renderer string, so a mediaId like '../../../Users/x/anything.webm' resolves outside the media dir and is unlinked; the test at __tests__/voice-note.test.ts:102 claims 'must not throw or escape' but only exercises a nonexistent file, so it passes hollow. *Scenario:* Any compromised or scripted renderer calls window.api.assistant.discardVoiceNote('../../<relative path>.webm') and deletes an arbitrary .webm/.ogg file anywhere the app user can write — e.g. call recordings elsewhere in userData — with the error swallowed by the catch.
- **[low] E-voice** — Message-cap trimming and pre-delete read leak voice-note media files (`src/main/assistant/conversations-fs.ts:373`). appendTurn trims to MAX_MESSAGES with slice(-MAX_MESSAGES) without deleting the voiceNote files of the messages it drops, and deleteConversation's cleanup (assistant-ipc.ts:315-321) iterates only the messages still present in the conversation it reads, so audio for trimmed-away messages is never deleted by any path. *Scenario:* A long-lived conversation passes 500 messages: each new turn silently drops the oldest messages, and every voice note among them becomes a permanently orphaned webm in conversations/media that even deleting the conversation no longer cleans up.
- **[high] F-learning-exclusion** — Forget loop silently skipped when memory db is null, but handler still returns ok:true (`src/main/assistant/assistant-ipc.ts:504`). assistant:setSalesBrainExcluded persists the flag, then guards its retroactive-forget loop on bare getMemoryDb() (assistant-ipc.ts:504-509) — which is null whenever Sales Brain is off or init failed/hasn't retried (memory-runtime.ts:145-147, and :151-158 documents that null can persist a whole session) — and returns {ok:true} at :511 either way, so the pill flips to 'Not learning' while the promised deletion never happened and never re-runs. *Scenario:* Startup init loses its 15s race (the exact case memory-runtime.ts:151-158 documents), or the user toggled Sales Brain off after this conversation taught memories. User clicks the pill and confirms the dialog that says 'Anything it already taught the Sales Brain will be forgotten' (AssistantView.tsx:591). Flag is set, delete loop is skipped, ok:true returned. Later the db comes back (a chip click runs ensureMemoryDb, or SB is re-enabled) — the taught memories are live again, in a conversation permanently marked excluded, and nothing ever re-runs the forget. Asymmetry makes it worse: the memory-WRITE path applySuggestion retries via getMemoryDb() ?? ensureMemoryDb() (assistant-ipc.ts:428); the consent-DELETE path does not. The call-level analog (memory-center-ipc.ts:129-136) has the same skip shape.
- **[high] F-learning-exclusion** — 'Not learning' does not suppress the Save-to-Sales-Brain chip path at any layer (`src/main/assistant/assistant-ipc.ts:423`). On an excluded conversation, chips are still generated every turn (assistant-ipc.ts:232-238 runs extractContextSuggestions with no exclusion or SB gate), still rendered (AssistantView.tsx:230-252 never reads chat.learningExcluded), and still applied — assistant:applySuggestion checks isSalesBrainEnabled (:423) but never re-reads conversation.salesBrainExcluded, so a tap writes a memory with evidence callId assistant:<conversationId> (:436-443) into a conversation whose pill says 'not learning from this conversation' (tooltip, AssistantView.tsx:584). *Scenario:* Two concrete paths. (1) Coherence: user excludes a conversation, keeps chatting; chips keep appearing and taps keep writing memories tagged to the excluded conversation — Memory Center then shows facts sourced from a conversation the user told the app not to learn from, and a later pill off→on toggle silently deletes those explicitly-saved memories via the :506-508 loop. (2) The founder's queued-slow-chip race: user taps a chip; applySuggestion enters consolidateNewCandidate's embedding + AI-judge calls (consolidation.ts:217-231); user clicks 'Not learning'; the forget loop runs finding nothing; the chip's insert lands afterwards (consolidation.ts:232) — a memory survives the forget the user just confirmed. An explicit tap is arguably its own consent, but the code never made that call: the handler simply never reads the flag. At minimum applySuggestion should re-read exclusion before insert; whether chips render at all on an excluded conversation needs an explicit product decision.
- **[high] F-learning-exclusion** — Deleting a conversation strands its taught memories with no bulk-forget path left (`src/main/assistant/assistant-ipc.ts:304`). assistant:deleteConversation (assistant-ipc.ts:304-323) deletes the file and voice notes but never touches memories tagged assistant:<id>, and after deletion the exclusion handler can never forget them: setConversationSalesBrainExcluded returns null on a missing file (conversations-fs.ts:413-415 via getConversation), so assistant-ipc.ts:502 returns {ok:false} before the delete loop ever runs. *Scenario:* User has a conversation that taught the Sales Brain several memories, deletes it ('Delete this conversation? This cannot be undone.' — AssistantView.tsx:491; conversations-fs.ts:330 says 'delete means delete'). The memories persist and keep feeding retrieval, citing 'Said in a chat with Rise' (AssistantView.tsx:312-313) for a conversation that no longer exists — and the one bulk-forget mechanism (the pill) is gone with it; only hunting each memory down in Memory Center remains. Also the in-flight interaction the audit brief asked about: the hook re-reads the conversation only once at start (memory-hooks.ts:173-177) — a delete landing after that read but before consolidation produces memories orphaned at birth; the abort at :309-311 covers only the streaming turn, not a pending extraction from the just-finished turn, which has no cancel signal at all. Delete should either run the same forget loop exclusion does, or explicitly tell the user the Sales Brain keeps what it learned.
- **[low] F-learning-exclusion** — Pill shows 'Learning' with Sales Brain master flag off; 'Saved' chips survive the retroactive forget (`src/renderer/src/features/assistant/AssistantView.tsx:575`). The learning pill renders unconditionally (AssistantView.tsx:575-614) with no isSalesBrainEnabled check anywhere in the renderer, so a user with Sales Brain globally off sees an active 'Learning' pill claiming Rise 'can save facts from this conversation' while the hook no-ops at memory-hooks.ts:169; relatedly, chips tapped with SB off silently return ok:false (assistant-ipc.ts:423) with zero UI feedback, and appliedSuggestionIds are never cleared by exclusion, so a chip still reads 'Saved' (AssistantView.tsx:247-248) after the forget loop deleted the memory behind it. *Scenario:* Sales Brain disabled in Settings: the Rise header still shows 'Learning' (false in the safe direction, but it misstates consent state), and tapping a Save-to-Sales-Brain chip does nothing visible — a dead button. Separately, after a user excludes a conversation (deleting its taught memories via assistant-ipc.ts:506-508), any previously applied chip still displays 'Saved' for a memory that no longer exists.
- **[high · field] G-ux** — Rise pane never fills the viewport: h-full dies at MainApp's animate-view wrapper (`src/renderer/src/app/MainApp.tsx:335`). AssistantView's root `h-full` (AssistantView.tsx:508) resolves to auto because its parent `<div key={active} className="animate-view">` (MainApp.tsx:335) has no height, inside AppShell's `flex-1 overflow-y-auto px-8 py-7` scroll container (AppShell.tsx:49) — so the rail and chat cards size to content instead of filling the window, leaving the dead band the founder screenshotted, and both internal scroll areas (AssistantView.tsx:532 rail, :616 messages) never activate: the whole page scrolls instead. *Scenario:* Open Rise in a 1006px-tall window: cards end ~70% down with a large empty band below and the composer floating mid-window. Start a long conversation: the chat card grows past the viewport, the AppShell page scrolls, the autoscroll effect (AssistantView.tsx:441-443, scrollTo on a container whose scrollHeight equals clientHeight) is a no-op, so during streaming new text grows below the fold and the user must manually chase the page scrollbar to read the answer and reach the composer; a long conversation list likewise stretches the page instead of scrolling the rail.
- **[high · field] G-ux** — Assistant replies render markdown as literal text (whitespace-pre-wrap, no renderer) (`src/renderer/src/features/assistant/AssistantView.tsx:108`). CitedText renders the model's reply via `whitespace-pre-wrap` with no markdown rendering (AssistantView.tsx:108), and the system prompt (src/main/assistant/context.ts:21 — 'Plain language, no filler' is tone, not formatting) never instructs the model to avoid markdown, so `**bold**`, `# headers`, `- bullets`, and code fences appear as raw punctuation. *Scenario:* User clicks the shipped starter prompt 'What should I focus on this week?' (AssistantView.tsx:48) — a question every model answers as a formatted list — and the first answer they ever see is littered with literal asterisks and dashes, e.g. '**1. Follow up with Dana** - ...', making Rise look broken on first contact.
- **[high] G-ux** — Chat column collapses to ~112px at the app's own minimum window width (`src/renderer/src/features/assistant/AssistantView.tsx:510`). Every horizontal width around the chat pane is fixed with no breakpoint: sidebar w-60 (AppShell.tsx:35), page padding px-8 (AppShell.tsx:49), rail w-72 shrink-0 (AssistantView.tsx:510), gap-4 (:508), copilot w-80 (AppShell.tsx:57); the window minWidth is 1040 (src/main/index.ts:244), leaving 1040-240-64-288-16-320 = 112px for the entire chat column (min-w-0 lets it shrink that far). *Scenario:* User on a 1366x768 laptop gets a 438px chat pane (341px bubbles); user who narrows the window toward the allowed 1040px minimum with the copilot expanded gets a 112px chat column where the composer row (textarea + mic + Send at fixed padding) is physically unusable and bubbles are ~87px wide — the rail never collapses and nothing reflows.
- **[medium] G-ux** — No streaming progress before the first token: bare blinking cursor for up to 45s (`src/renderer/src/features/assistant/useAssistantChat.ts:156`). send() creates an empty assistant bubble immediately (useAssistantChat.ts:156-166) whose only content is the pulse cursor (AssistantView.tsx:199-201), but per the M28 design the answer stream starts only after a forced plan_research call plus lookups (docs/M28-rise.md decision 6; 45s per-attempt budget, decision 8), with no interim status ever shown. *Scenario:* User asks about a client; the plan call plus call-search lookups take 10-40s on a slow provider or free-tier pacing; the user watches an empty bubble with a blinking cursor with no 'searching your calls...' indication, concludes Rise hung, and hits Stop — which (before any token) silently discards the whole turn (useAssistantChat.ts:181-183).
- **[medium] G-ux** — Forced scrollTo-bottom on every message change will hijack reading during streaming (`src/renderer/src/features/assistant/AssistantView.tsx:441`). The effect at AssistantView.tsx:441-443 calls scrollTo(bottom) on every `chat.messages` identity change — which is every streaming delta (useAssistantChat.ts:122-124 maps a new array per delta) and every suggestion/task state update — with no 'user has scrolled up' guard; today it is dead (container never overflows, see the h-full finding), and once heights are fixed it will yank the view to the bottom several times per second. *Scenario:* During a long streamed answer the user scrolls up to re-read an earlier message; every incoming delta instantly snaps the pane back to the bottom, making earlier content unreadable until streaming ends.
- **[medium] G-ux** — Empty state: EmptyState's py-16 detaches the suggestion chips ~84px below the copy (`src/renderer/src/features/assistant/AssistantView.tsx:555`). The empty state uses EmptyState without `compact` (AssistantView.tsx:555-559), inheriting `py-16` (EmptyState.tsx:39), so its 64px bottom padding plus the chip row's mt-5 (AssistantView.tsx:560) inserts ~84px of dead space between the 3-line description and the starter chips, producing the disconnected floating-chips row in the founder's screenshot; the block's vertical position also depends on the rail's content height (root `flex` items-stretch, :508), so it wanders as conversations accumulate. *Scenario:* First launch of Rise: the icon/title/body read as one group and the three chips read as an unrelated strip near the composer; users don't recognize them as the suggested starting points for the block above.
- **[medium] G-ux** — Evidence modal is off-system: no padding, no visible title, no close button, long evidence clipped unreachably (`src/renderer/src/features/assistant/AssistantView.tsx:278`). EvidenceModal passes raw content to Modal with zero horizontal padding (AssistantView.tsx:280-331 have only py-*; house convention is a px-6 py-4 wrapper — MemoryReviewModal.tsx:29, ViewCallModal.tsx:43), the 'Where this comes from' title is sr-only (Modal.tsx:64-67) so the dialog opens with no visible heading or close control, and unlike ViewCallModal's `max-h-[85vh]` + internal scroll it sets no height cap while the Modal panel is `overflow-hidden` (Modal.tsx:59). *Scenario:* User taps a citation for a memory with many transcript quotes: statement text runs edge-to-edge against the rounded border, and if the evidence list is taller than the window the panel outgrows the viewport with overflow-hidden and no scrollbar — the bottom quotes and the whole top are unreachable; a user who misses backdrop-click/Escape has no visible way to close it.
- **[medium] G-ux** — Conversation rail rows are mouse-only: no keyboard access, actions hidden behind hover (`src/renderer/src/features/assistant/AssistantView.tsx:353`). ConversationRow is a plain div with onClick (AssistantView.tsx:353-358) — no role, tabindex, or key handling — and its Rename/Delete controls are `hidden group-hover:flex` (:390), so selection is unreachable by keyboard and the actions are undiscoverable without a precise hover, violating the app's own focus-ring commitment (index.css:122-125 only helps elements that are focusable at all). *Scenario:* A keyboard user tabs through Rise: focus jumps from the search field straight to the chat pane; they can never open, rename, or delete a past conversation; on any touch/pen input the rename and delete affordances simply do not exist.
- **[medium] G-ux** — Voice note silently discarded when sent as the first message of a new conversation (`src/renderer/src/features/assistant/AssistantView.tsx:472`). handleSend on the no-active-conversation path deliberately discards the attached voice note (AssistantView.tsx:472-476 calls discardVoiceNote) right after the UI promised the opposite — the pending chip says 'Voice note attached · 0:42 — review the text below, then send.' (:651-658) — with no warning that the audio will not be kept. *Scenario:* New user's very first Rise interaction: records a voice note from the empty state, sees 'Voice note attached', hits Send; the transcript text sends but the audio is deleted, and the sent message shows no playback chip — the attachment they were just told about vanished without explanation.
- **[low] G-ux** — No max text width: message column stretches full-pane on large windows (`src/renderer/src/features/assistant/AssistantView.tsx:178`). Bubbles are `max-w-[78%]` of the chat pane (AssistantView.tsx:178) with no column cap, while the app's other reading surfaces center at max-w-3xl (CoachingView.tsx:132, CallDetail.tsx:770); at the founder's 1897px window the pane is ~1000px so assistant paragraphs run ~780px, and on wider monitors past 1200px lines exceed comfortable measure entirely. *Scenario:* Maximized on a 2560px monitor with the copilot collapsed: the chat pane is ~1800px, assistant replies wrap at ~1400px — 200+ character lines that are hard to read and visually inconsistent with Coaching's centered column.
- **[low] G-ux** — Citation chips renumber replies and can false-match bracketed text (`src/renderer/src/features/assistant/AssistantView.tsx:98`). CitedText displays each chip as its first-occurrence index plus one (AssistantView.tsx:88-101 `{idx! + 1}`), not the marker's own number, so a reply citing [3] before [1] shows chips labeled 1 and 2 — contradicting any textual reference the model makes to its own numbers — and during streaming (citations undefined until the post-turn refetch) the raw [n] markers show as plain text then morph into differently-numbered chips; the split regex also converts any literal [1]/[12] in ordinary prose into a chip when citations exist. *Scenario:* Model writes 'Their budget is tight [3]. As noted in [3], ...' — the chip renders as '1' while the prose says 'in [3]'... wait, the second [3] also becomes chip '1', but a reply that says 'point [2] above' where [2] was never a citation marker in first position gets a chip stolen from the wrong evidence; separately the user watches markers visibly renumber the instant streaming completes.
- **[low] G-ux** — 10px and 10.5px type below the design system's own 11px metadata floor (`src/renderer/src/features/assistant/AssistantView.tsx:98`). The citation chip uses text-[10px] (AssistantView.tsx:98) and the rail timestamp text-[10.5px] (:386) while the token file's own comment sets 11px as the floor for real metadata ('--color-faint ... real 11px metadata, must clear AA', index.css:19). *Scenario:* On a standard 100%-scale 1080p display the tappable citation chips and conversation timestamps render at 10px — smaller than anything else in the app — hurting legibility of exactly the trust-critical element (citations) M28 was built around.
- **[low] G-ux** — Composer polish cluster: frozen Transcribing spinner, fixed 2-row textarea, controls outside the field, native confirm dialogs (`src/renderer/src/features/assistant/AssistantView.tsx:698`). Four off-system details in one area: (a) the 'Transcribing…' button passes Loader2 through Button's icon prop (AssistantView.tsx:698-700) which adds no animate-spin (Button.tsx:68), so the spinner is frozen; (b) the textarea is a fixed rows={2} fieldClass box with no autogrow (:685-687) and Mic/Send float outside it as siblings (:684-719), the detached-composer look the founder flagged; (c) Enter-to-send (:692) has no isComposing guard, sending mid-IME-composition; (d) destructive/consequential confirms use native window.confirm (:491, :589-595) instead of the app's Modal, and the learning pill's entire explanation lives in a hover-only title tooltip (:583-585). *Scenario:* User taps mic, finishes recording: the 'Transcribing…' button shows a motionless loader that reads as hung; a user typing a long multi-paragraph question scrolls inside a 2-line box; a Japanese-IME user pressing Enter to commit a conversion fires the send; deleting a conversation pops the bare OS confirm dialog that matches nothing else in the app.
- **[high] H-a11y** — Conversation rows and their rename/delete actions are keyboard-unreachable (`src/renderer/src/features/assistant/AssistantView.tsx:390`). The conversation row is a plain div with onClick (no role, no tabIndex — AssistantView.tsx:353-359) and its rename/delete IconButtons sit in a 'hidden group-hover:flex' container (:390), so display:none removes them from the tab order; a keyboard or screen-reader user cannot select, rename, or delete any past conversation, which deviates from the app's own accessible hover pattern ('opacity-0 group-hover:opacity-100 group-focus-within:opacity-100' in TasksView.tsx:287, ContactsView.tsx:357, KnowledgeView.tsx:163) and from PipelineBoard.tsx:214-215 which gives clickable divs role="button" tabIndex={0}. *Scenario:* A keyboard-only user opens Rise, presses Tab: focus reaches New chat, the search input, then jumps past the entire conversation list into the chat pane. They can never re-open yesterday's conversation, never rename it, and never delete it — the history rail is mouse-only.
- **[medium] H-a11y** — No aria-live on streaming replies or error banners — screen readers hear nothing (`src/renderer/src/features/assistant/AssistantView.tsx:637`). The chat error banner (:637-641), the voice error banner (:645-649), and the streaming message region (:616) have no aria-live, role="alert", or role="status", even though the app's established pattern uses them (AuthScreen.tsx:35 role="alert", :47 aria-live="polite"; ToastProvider.tsx:52; SuggestionRail.tsx:44; CueCard.tsx:111), so assistive tech is never notified of the assistant's answer, a failed turn, or a mic-permission error. *Scenario:* A screen-reader user sends a message; the reply streams in and completes in silence, or the turn fails and the red 'error' banner appears with no announcement — the user sits waiting, believing nothing happened, and has no cue to go hunting for the banner or the reply text.
- **[medium] H-a11y** — Citation chips: 16px hit target, 10px font, accessible name is just a bare number (`src/renderer/src/features/assistant/AssistantView.tsx:98`). The citation chip button (:93-102) is styled 'h-4 min-w-4 ... text-[10px]' — a 16px hit target (below the WCAG 2.5.8 24px minimum) with 10px text — and its accessible name computes from its content, the bare digit (the title attribute at :97 loses to content in the accname algorithm), so a screen reader announces only '1, button' with no hint it opens evidence. *Scenario:* Any user tries to tap/click the tiny superscript chip and misses (16px target inline in body text); a screen-reader user tabbing through a reply hears 'button, 1' 'button, 2' with no indication these open the evidence modal — the entire every-claim-traceable feature is undiscoverable non-visually.
- **[medium] H-a11y** — Evidence modal has no close button and, while loading, no focusable element — initial focus never enters it (`src/renderer/src/features/assistant/AssistantView.tsx:278`). EvidenceModal (:259-335) renders no close control, and in its 'loading' state (:279-282) — and in any loaded state without an 'Open the call' link — the panel contains zero focusable elements; useModalA11y.ts:30-31 then falls back to panel.focus() on a div that has no tabindex (a no-op), so focus stays on the citation chip behind the open modal, and Tab is swallowed by the trap's empty-items branch (useModalA11y.ts:44-47), leaving Escape or backdrop-click as the only, undiscoverable, exits. *Scenario:* A user opens a citation whose evidence has no call link (e.g. a memory sourced from onboarding or with no quotes): the modal shows over the chat, Tab does nothing, no close button exists anywhere in the panel, and a keyboard user who doesn't guess Escape is stuck; a screen-reader user is never moved into the dialog at all because nothing inside it receives focus.
- **[low] H-a11y** — window.confirm used for delete-conversation and stop-learning instead of the app's Modal pattern (`src/renderer/src/features/assistant/AssistantView.tsx:491`). Deleting a conversation (:491) and disabling learning (:590-593) — both irreversible, the second explicitly 'cannot be undone' since it forgets learned memories — go through native window.confirm rather than the app's Modal + useModalA11y shell (Modal.tsx:22-27) that every other dialog uses, losing focus restoration, styling, and the danger-variant affordance (MemoryCenterSection.tsx:74,:179 shares this existing debt, so it is pattern-inconsistent rather than novel). *Scenario:* A user triggers 'Delete this conversation' and gets an unstyled OS-chrome dialog that looks nothing like the rest of the app; after dismissing it, focus is wherever Chromium left it rather than restored to the triggering control, and the destructive action has no visual weight distinguishing it from a benign confirm.
- **[low] H-a11y** — Recording flow: focus dropped on state swap, no Escape to cancel, recording/level state visual-only (`src/renderer/src/features/assistant/AssistantView.tsx:660`). When recording starts, the composer block (:684-719) containing the just-activated Mic IconButton (:703-708) unmounts and is replaced by the recording bar (:660-682) with nothing refocused, so keyboard focus falls to body; no Escape handler cancels recording (the only key handling in the view is the textarea's Enter at :691-696 and the rename input's at :367-373); and the recording state is conveyed by a red pulsing dot (:662-665) and a width-styled level bar (:670-675) with no aria — though the elapsed-time text at :666-668 keeps it from being purely color-coded. *Scenario:* A keyboard user presses Enter on the mic button; the button vanishes, focus drops to the document body, and they must Tab from the top of the window to reach Cancel/Done; pressing Escape — the reflex for 'stop this' — does nothing, and a screen-reader user gets no announcement that recording has started or that the mic is picking up audio.
